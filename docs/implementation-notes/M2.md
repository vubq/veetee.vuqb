## Đã có trong host-native control-plane slice

- Fastify API có runtime health, assistant role draft, provider catalog/config,
  model-memory selection, publish transaction fixture và machine runtime snapshot
  với ETag.
- Manager Web có HTTP gateway, provider registry schema-driven form và nút
  `Áp dụng runtime`; không bật API URL thì preview fixture vẫn chạy độc lập.
- Provider Registry Web phân biệt create/update: config đã tồn tại được gửi
  `PATCH` kèm `If-Match` để tạo immutable revision tiếp theo, không POST bản
  trùng mỗi lần chỉnh. POST chỉ dùng khi chưa có config cho installation.
- `tools/runtime/manifests/host-native-dev.json` khởi động API + Voice Server + Web
  và readiness-check theo dependency. Voice Server manager-source đã nhận publish
  revision mà không restart.
- Typecheck/lint/unit/E2E Web đều pass; API build/test pass.
- Persistence slice dùng Drizzle typed schema + reviewed SQL migration. Database
  riêng `veetee_vubq` chạy trên instance `127.0.0.1:55432`; schema revision gồm
  `assistant_revision`, `provider_config_revision`, `runtime_publication` theo
  assistant, secret reference/binding metadata, session và audit tables.
- API restart test đã xác nhận role/published snapshot còn nguyên sau process
  restart; migration one-shot chạy trước API trong manifest PostgreSQL riêng.
- Local auth slice đã có opaque cookie, PostgreSQL-backed session, `/auth/me`,
  logout, exact Origin + CSRF gate; encrypted local secret store dùng AES-256-GCM
  và response chỉ trả metadata/reference. Các lần ghi file được serialize và
  secret reference vẫn `RESOURCE_IN_USE` nếu từng xuất hiện trong một immutable
  provider-config revision, để không làm hỏng lịch sử cấu hình.

## OpenAPI route contract — current slice

- `/openapi.json` hiện được tạo bởi `@fastify/swagger@9.8.1` từ route schema đã
  đăng ký trong Fastify, thay cho document viết tay trước đó. Transform chung
  bổ sung operation ID/tag/security metadata mà không tạo route hoặc behavior
  riêng cho Web.
- Contract test `OpenAPI is generated from every registered route` kiểm tra đủ
  28 path public/internal hiện có, operation ID, response entry, common `400`/
  `500` error và auth `401`/`403` response cho route có security; hai security
  scheme là (`veeteeSession`, `machineBearer`). Runtime probe trên API `18101`
  trả OpenAPI `3.1.0` và path count `28`.
- Snapshot lấy từ endpoint runtime đã được `@redocly/cli@1.34.0 lint` xác nhận
  hợp lệ, không warning sau khi khai báo private-license identifier.
- Artifact `veetee-manager-api/openapi/manager-api.json` có lệnh
  `npm run openapi:export` và `npm run openapi:check`; check so sánh deep-equal
  với `app.swagger()` từ fixture Fastify, nên file stale sẽ fail rõ ràng.
- Manager Web có `npm run api:generate` dùng `openapi-typescript@7.13.0` để tạo
  `src/api/generated.ts`. HTTP gateway đã dùng các request type generated cho
  assistant/role/provider/model-memory writes; typecheck, lint, 12 unit tests và
  production build pass.
- Đây là bước chống drift và route/error coverage. Role/runtime policy payload
  vẫn có object extension có chủ đích vì prompt/personality/provider config là
  schema-driven; generated HTTP operation client đầy đủ và error examples theo
  từng domain code vẫn là gate M2 chưa được tuyên bố đạt.

## Response contract slice (2026-08-04)

- Đã thay response generic ở các route chính bằng JSON Schema allow-list: auth,
  secret reference, provider installation/config, assistant, model-memory,
  publication, voice, health và machine runtime snapshot. Dynamic policy/config
  fields vẫn dùng object schema có `additionalProperties` có chủ đích, còn các
  metadata/identity fields đều được liệt kê rõ.
- Fastify serializer hiện tự loại field không thuộc response contract. Trong lúc
  bật gate này, regression đã phát hiện và sửa lỗi InMemory store làm mất
  `locatorMasked` khi PATCH secret reference không gửi locator; trước đây lỗi bị
  che vì route không có response schema.
- OpenAPI artifact đã export/check và Redocly lint pass; Manager Web đã regenerate
  `src/api/generated.ts`, typecheck/lint/Vitest **12/12** và production build
  pass. Manager API PostgreSQL suite **10/10**, lint/build pass.
- Đây là response-contract gate, chưa tuyên bố M2 hoàn tất: generated operation
  client đầy đủ, device pairing, history/retention và các response/error examples
  chi tiết vẫn còn trong roadmap.

## Device pairing slice (2026-08-04)

- Route `POST /api/v1/devices/pair` không còn trả `501`. Luồng mới gồm machine
  endpoint `POST /internal/v1/devices/pairing-challenges` tạo challenge một lần,
  trả mã `VT-xxxx` đúng một lần; owner nhập mã để bind device vào assistant.
  Code chỉ lưu SHA-256 hash, có expiry 10 phút và trạng thái `pending → used`;
  không lưu raw code hoặc identity hash trong public response.
- Data model PostgreSQL thêm `device` và `pairing_challenge` trong migration
  `003_device_pairing.sql`, với unique `(identity_hash, client_id_hash)`, owner/
  assistant binding và index theo trạng thái/expiry. InMemory store dùng cùng
  semantics để preview/test không lệch production.
- `GET /api/v1/assistants/{id}/devices` trả device card allow-list; response chỉ
  có masked MAC, board, firmware, trạng thái và timestamps. Machine challenge
  request được redact `verificationCode` trong logger policy.
- Test evidence: Manager API **12/12** pass, gồm challenge single-use ở memory
  và PostgreSQL persistence/restart; migration `veetee_manager` chạy thành công.
  OpenAPI/Redocly và Web generated contract cập nhật; Web typecheck/lint/unit
  **12/12** và build pass.
- Đây chưa phải physical pairing acceptance: firmware chưa gọi challenge endpoint
  và voice/MCP device reporting vẫn là gate firmware/hardware tiếp theo.

## Generated HTTP operation client (2026-08-04)

- Manager Web nay dùng `openapi-fetch` với `paths` được sinh từ
  `veetee-manager-api/openapi/manager-api.json`; gateway gọi các operation typed
  (`GET`/`POST`/`PATCH`) với path params, query params, request body và response
  type lấy trực tiếp từ artifact. Các cast `fetch` + `unknown` cho route chính đã
  được loại bỏ; mapping từ API resource sang domain UI chỉ còn ở một boundary.
- Client có middleware session dùng `credentials: include`, tự lấy CSRF token từ
  `/api/v1/auth/me` khi cần và gắn `X-Veetee-CSRF` cho mutation. Login cập nhật
  token trong memory; không ghi credential/token vào `localStorage` hoặc
  `sessionStorage`. Network exception được chuyển thành problem offline hữu hạn,
  không queue mutation vô hạn.
- API bổ sung query contract cho assistant search, voices, provider configs và
  machine runtime config; assistant search filter chạy ở API thay vì nối chuỗi
  query trong Web. Model/memory response schema đã khai báo enum/oneOf để
  generated type bảo toàn provider kind và selected-vs-disabled invariant.
- Regression: API search validation, OpenAPI export/check và client CSRF tests;
  Manager Web typecheck/lint/Vitest **14/14** và production build pass. Đây là
  generated-client contract gate; login UI, rate-limit production và Voice
  Server history telemetry vẫn chưa được promotion.

## Login throttling (2026-08-04)

- Local auth có bounded process-local throttle theo ba bucket: IP, normalized
  identity và cặp IP/identity. Giới hạn `maxAttempts`, cửa sổ, lockout và số bucket
  đều là environment config (`VEETEE_LOGIN_*`), không phải literal trong route.
- Sai credential vẫn trả `401 INVALID_CREDENTIALS` cho tới khi gate đạt ngưỡng;
  sau đó trả `429 LOGIN_THROTTLED` cùng `Retry-After`. Identity không tồn tại và
  identity tồn tại không có response code riêng; Argon2 verify được thực hiện trên
  hash owner khi có cấu hình để giảm timing distinction. Thành công xoá bucket của
  IP/identity; map có eviction để không tăng vô hạn.
- Unit/API tests chứng minh lockout hết hạn, bounded map, IP rotation không né
  được gate, valid login bị chặn trong lockout và OpenAPI có response `429`.
  PostgreSQL session/pairing regression vẫn pass **16/16**.
- Đây mới là baseline single-process protection cho local host; distributed
  throttling hoặc reverse-proxy rate limit chỉ được thêm khi có deployment/ADR
  tương ứng. Login UI và history production hardening vẫn là phần M2 chưa
  promotion.

## Conversation history và retention slice (2026-08-04)

- Manager API đã có domain contract cho `RetentionPolicy`, conversation summary,
  transcript segment, timing/tool metadata và conversation detail. Route public
  gồm `GET/PATCH /api/v1/retention-policy`,
  `GET /api/v1/assistants/{id}/conversations` và
  `GET /api/v1/conversations/{id}`; Voice Server ingest dùng
  `POST /internal/v1/conversations/turns` với machine bearer.
- Migration `004_conversation_history.sql` đã chạy trên PostgreSQL riêng
  `veetee_vubq` (port `55432`). Transcript bật mặc định 30 ngày; audio tắt và
  PATCH cố bật audio trả `422 AUDIO_RETENTION_UNSUPPORTED` vì artifact audio
  chưa có contract lưu trữ.
- InMemory và PostgreSQL store cùng kiểm tra UUID/sequence/timestamp/giới hạn
  payload, giữ idempotency theo `turnId`, purge record hết hạn và không trả dữ
  liệu của owner khác. Bounded reporter của Voice Server được nối ở ngoài audio
  critical path; queue đầy chỉ tăng drop metric, không chặn ingest/provider.
- Evidence: Manager API PostgreSQL + unit suite **18/18**, lint/build và
  OpenAPI export/check pass. Manager Web history route có retention notice,
  loading/empty/detail states; preview thêm scenario dữ liệu bounded để kiểm tra
  detail và keyboard `Enter`/`Space`; typecheck/lint/Vitest **15/15**, production
  build và Chromium E2E **8/8** (gồm axe serious/critical gate) pass.
- Đây là persistence/UI slice cộng telemetry host-native, chưa phải M2 production
  DoD: retention job/backup, secure machine bearer provisioning và physical device
  reporting chưa promotion. Runtime PostgreSQL manifest dùng DB `veetee_vubq`;
  fixture memory vẫn được giữ riêng cho M0/M1 oracle.

## Voice Server history reporter (2026-08-04)

- `veetee-server/src/veetee_server/history.py` dùng `asyncio.Queue` bounded,
  `put_nowait`, retry cùng một endpoint cho lỗi `429/5xx` và shutdown drain có
  deadline. Không có provider fallback, không lưu audio và không ghi payload vào
  log. Tool history chỉ lưu shape/key metadata, không lưu giá trị arguments/output.
- Mỗi WebSocket conversation có UUID và sequence; mỗi turn gửi transcript user /
  assistant, `configRevision`, `finishReason`, timing (`ASR`, first LLM token,
  TTS start, TTFA, duration) và trạng thái active/completed/aborted/error. Event
  được idempotent ở Manager API theo `conversationId + turnId`.
- Runtime probe sau restart manifest: Voice `18100`, API `18101`, Web `18181`,
  PostgreSQL `55432` đều ready; một synthetic WS v3 turn không phát audio vật lý
  trả `135–136` downlink packets, TTFA khoảng `1,58–1,61 s`, metrics
  `history_enqueued=1`, `history_sent=1`, `history_failed=0`, và detail API có
  transcript/timings không rỗng.
- Cùng lượt đã phát hiện và sửa supervisor Node symlink bug: resolver không còn
  `Path.resolve()` trên symlink `npm`, tránh PATH giả `bin/node_modules/npm/bin`
  làm migration thất bại. Runtime regression tăng lên **8 tests passed**.

## Device presence reporting slice (2026-08-04)

- Firmware hello có optional additive `device_info.board` và
  `device_info.firmwareVersion`; framing, required hello fields và peer cũ không
  đổi. Voice Server validate metadata, mask MAC và gửi SHA-256 identity/client
  tới `POST /internal/v1/devices/presence` qua queue bounded riêng với history.
- Manager API lưu/update row theo `(identity_hash, client_id_hash)`, giữ owner /
  assistant binding nếu thiết bị đã pair, và cập nhật `online`/`offline`, board,
  firmware, `lastSeenAt`. Raw `Device-Id`/`Client-Id` không được lưu hay log.
- Evidence: Manager API memory **13 passed**; PostgreSQL suite với
  `VEETEE_TEST_DATABASE_URL_FILE` **20/20 passed** (gồm presence restart),
  Voice Server **37 passed**, firmware host CTest **1/1**. OpenAPI đã export/check
  với route internal presence mới.
- Runtime manifest PostgreSQL đã bật `VEETEE_PRESENCE_ENABLED=true`. Physical
  acceptance cho additive hello đã hoàn tất sau khi flash không erase ở
  `/dev/ttyACM0`: serial thấy app `b133e61`, `device_info` được gửi sau hello,
  PSRAM 8 MiB, WakeNet model sẵn sàng, LCD `ST7789 240x280`, Wi-Fi profile từ
  NVS và `server hello accepted; session ready`. Voice metrics ghi
  `presence_enqueued=1`, `presence_sent=1`; PostgreSQL `veetee_vubq` có 17 device
  rows, 3 unpaired presence rows và `last_seen_at` mới nhất khớp lượt flash.
  Không phát audio và không đổi Wi-Fi.

## Machine bearer provisioning hardening (2026-08-04)

- Host-native manifest có one-shot `veetee-machine-token` dùng
  `tools/runtime/ensure_secret.py`: tạo token ngẫu nhiên khi thiếu, giữ file
  owner-only `0600`, không in token và không ghi vào Git. Token path bị ignore
  bằng `secrets/*.machine-token`.
- Manager API giờ bắt buộc bearer nếu PostgreSQL runtime không bật explicit
  `VEETEE_ALLOW_INSECURE_LOCAL_CONFIG=true`; Voice Server đọc cùng token file cho
  runtime-config, history và presence. Memory/unit fixtures vẫn phải ghi rõ
  insecure mode hoặc token test, nên không làm yếu production path.
- Runtime recovery sau restart: migration pass, API `18101`/Voice `18100`/Web
  `18181` ready revision `62`, Voice `lastActivationErrorType=null`; counter
  `activationFailures=5` là tích lũy từ source reload transient trước readiness,
  không có failure đang hoạt động. Probe internal
  endpoint trả `401` khi thiếu/sai bearer và `200` khi token đúng. Token file
  mode `600`, không đưa giá trị vào note/log.
- Evidence: runtime tools **12 passed**, Manager API memory **15 passed** và
  PostgreSQL **22/22** khi bật `VEETEE_TEST_DATABASE_URL_FILE`; không phát audio,
  không flash/reset board.

## Retention job và PostgreSQL backup rehearsal (2026-08-04)

- Manager API có purge job bounded theo `VEETEE_RETENTION_INTERVAL_SECONDS`
  (mặc định 1 giờ) và machine-only `POST /internal/v1/retention/purge` để vận
  hành/kiểm thử tường minh. Store memory và PostgreSQL đều xóa conversation hết
  `retentionUntil`; foreign-key cascade xóa turn transcript/tool metadata đi kèm.
- `tools/runtime/backup_postgres.py` tạo custom-format dump bằng binary PostgreSQL
  local, mode output `0600`, chạy `pg_restore --list`, và với `--rehearse` tạo
  database tạm random, restore, kiểm tra schema `veetee_manager`, rồi drop đúng
  database tạm. DSN/credential không in ra output.
- Evidence: purge unit/API test pass; backup rehearsal thật tạo
  `.runtime/backups/veetee_vubq-retention-rehearsal-20260804T032000Z.dump`
  **61,140 bytes**, restore check `veetee_manager` pass và temp database đã drop.
  Runtime tools tăng lên **12 passed**; main DB `veetee_vubq` không bị xóa/đổi tên.

## Dependency security verification

- `drizzle-orm` được nâng lên `0.45.2` để vượt advisory SQL-identifier escaping
  của các bản `<0.45.2`. `npm audit --omit=dev --audit-level=high` hiện báo
  **0 vulnerabilities**.
- Sau thay đổi: Manager API PostgreSQL test **10/10**, `npm run lint` và
  `npm run build` pass; không đổi database name/port hoặc runtime provider.

## Chưa đạt M2 production gate

Retention job/backup rehearsal, physical device presence và provider no-fallback
fault evidence đã có host-native evidence. Auth UI/route guard baseline đã có
evidence ở mục dưới. Các gate còn mở là OpenAPI response/error coverage đầy đủ
cho mọi route, accessibility acceptance critical flows, UI loading/empty/error/
offline state cho mọi route chính và provider activation/promotion evidence.
Voice Server history + presence telemetry đã chạy ở manifest PostgreSQL local; runtime
`host-native-dev.json` vẫn dùng memory fixture để không ảnh hưởng M0/M1.
Không coi fixture memory là production persistence.

## Evidence / rollback

Mốc dynamic config/API/Web đã push qua `0669de9`; reporter/presence đã có trong
`dee9aa7`, machine bearer trong `c2c0c4b`, và retention/backup rehearsal trong
`b133e61` trên `origin/master`. ADR-014 ghi isolation và revision model; rollback
source vẫn giữ nguyên như các mốc trước. Database mới chỉ có dữ liệu fixture/test
của mốc này, không liên quan database project khác.

## Realtime Lab hand-off (2026-08-04)

- Runtime tools hiện **16 passed**, gồm probe WebSocket v3 host-only và test
  report/timeout. Probe không phải đường audio vật lý: không mở sound device,
  serial hay microphone ESP32.
- Physical wake recognition vẫn được theo dõi ở M1; sau khi chủ dự án cấp phép
  `--allow-audio`, harness sẽ dùng clip wake đã cấu hình trước clip utterance.
  Không lưu transcript, raw audio hay secret vào report/repository.
- Không đánh dấu M2 production gate hoàn tất chỉ vì handshake/TTFA probe; các
  kiểm tra loa, mic, LCD, nút, wake false-accept/false-reject và acoustic
  barge-in vẫn cần evidence vật lý riêng.

## Manager Web authentication surface (2026-08-04)

- Thêm `LoginView.vue` dùng toàn bộ `VtInput`/`VtFormField`/`VtButton` đã có,
  text đi qua i18n `vi-VN`, loading/error/throttling/offline state có role phù hợp.
- `auth-session.ts` dùng cookie session do API cấp, giữ CSRF token trong HTTP
  client duy nhất; không lưu password, session token hoặc CSRF vào localStorage/Pinia.
  Router guard gọi `/api/v1/auth/me`, redirect về `/login?redirect=...` khi chưa
  xác thực, whitelist redirect chỉ cho path nội bộ. Top nav có logout.
- Target host-native manifest hiện `VEETEE_AUTH_MODE=disabled`, nên `/auth/me`
  trả local owner để smoke runtime không bị chặn; khi bật auth thật, cùng UI/guard
  dùng cookie + CSRF contract của Manager API.
- Unit suite Web hiện **18 passed**; typecheck, lint và production build pass;
  Chromium UI E2E hiện **8/8**. Đây là auth UI/route evidence, chưa phải
  production secret/password provisioning hoặc remote security gate.

## OpenAPI problem-response hardening (2026-08-04)

- `application/problem+json` response schema của Manager API đã chuyển từ
  `additionalProperties: true` sang allow-list `type`, `code`, `title`, `detail`;
  mọi lỗi API đều phải có `code`, không để secret/domain field rò qua serializer.
- OpenAPI transform khai báo error status chuẩn cho route có security:
  `400`, `401`, `403`, `404`, `409`, `422`, `428`, `429` (login) và `503`, cùng
  response description/schema; route health vẫn giữ schema readiness riêng.
- Contract test bắt buộc các status error này cho mọi public/internal secured
  operation. Artifact đã regenerate, `npm run openapi:check` pass và Redocly CLI
  lint pass. Manager API test **15 passed, 7 skipped**, TypeScript lint pass;
  Manager Web generated client được regenerate, typecheck/lint/unit **18 passed**
  và production build pass.
- Đây là schema/contract gate, không tuyên bố mọi route đã có UI error copy hoặc
  remote production auth; những gate đó vẫn cần evidence riêng.

## Regression after problem-response and runtime-pool mốc (2026-08-04)

- Manager API chạy với database thật `veetee_vubq` trên PostgreSQL `55432`:
  **22/22 passed**, gồm session/restart, publish revision, provider history,
  pairing, presence và conversation retention persistence.
- Firmware host protocol/state CTest build-current: **1/1 passed**. Không có
  source firmware/wire change trong các mốc này nên không flash hoặc reset board.
- Runtime sau reload vẫn giữ Voice/API/Web ready ở `18100/18101/18181`, Voice
  `activeConnections=1`; không đổi Wi-Fi/route máy tính và không ghi secret vào
  artifact.

## Private Tailscale Serve mapping (2026-08-04)

- Tailscale daemon/userspace tailnet đã được kiểm tra read-only; hostname lấy từ
  status là `veetee-dev.tail52a635.ts.net`. Đã thêm mapping additive
  `https://veetee-dev.tail52a635.ts.net:18443/` → Manager Web `18181`; các
  mapping Serve cũ không bị ghi đè.
- `AllowFunnel` không có port `18443`, không bật Funnel/public exposure và không
  chạy `tailscale up`; máy tính không đổi Wi-Fi, route hoặc NetworkManager.
- Curl từ chính host tới địa chỉ tailnet timeout vì daemon đang dùng userspace
  networking/self-route không loopback; cần peer tailnet khác để xác nhận remote
  TLS/HTML/WebSocket. Local `127.0.0.1:18181` và health API/Voice vẫn pass.

## Provider Registry resilience slice (2026-08-04)

- `ProviderRegistryFeature` trước đây chỉ có loading/selected/empty; lỗi từ
  `listProviderInstallations()` hoặc `listProviderConfigs()` bị bỏ qua nên có thể
  hiển thị editor một phần mà operator không biết snapshot chưa đầy đủ. Feature
  hiện dùng state machine rõ ràng `loading → ready | empty | error | offline`,
  retry bounded bằng request generation, và không mở editor khi một trong hai
  read request lỗi.
- Error summary có `role=alert`, focus programmatic vào heading `tabindex=-1`,
  `aria-busy` trên main và retry button dùng primitive `VtButton`. Mutation lỗi
  hoặc offline giữ nguyên name/JSON/secretRef draft, hiển thị lỗi inline bên cạnh
  action và chỉ dùng toast như kênh bổ sung.
- HTTP gateway phân biệt HTTP `500` (error/fresh) với network/`503`
  (offline/stale), tránh báo sai “ngoại tuyến” khi Manager API đang trả lỗi server.
  Không đổi provider selection, không thêm fallback và không đưa secret vào UI.
- Evidence: Manager Web `npm test` pass **27 unit tests**, production build pass;
  Provider Registry + HTTP gateway targeted suite **9/9**; Chromium preview E2E
  **8/8** và axe core surfaces không có violation `serious`/`critical`.
  Voice Server **50 passed**, firmware host CTest **1/1**, Manager API
  PostgreSQL **22/22**, runtime tools **16 passed**, ESP-IDF 6.0.2 build pass
  (67% app partition còn trống). Chỉ build/test; không flash, reset, phát audio,
  mở microphone hoặc thay đổi Wi-Fi/network.
- Đây là promotion evidence cho provider-registry route; M2 production gate vẫn
  còn các route UI khác, full OpenAPI domain error examples, accessibility matrix
  và provider activation/promotion evidence.

## Device List resilience slice (2026-08-04)

- `DeviceListFeature` trước đây chỉ phản hồi success/empty; `listDevices()` lỗi
  bị rơi thẳng xuống empty state, làm operator có thể tưởng robot chưa pair.
  Feature hiện phân biệt `loading`, `ready`, `empty`, `error` và `offline`, giữ
  toolbar/retry action ổn định và chỉ render danh sách khi read thành công.
- Error card có `role=alert`, focus vào heading `tabindex=-1`, `aria-busy` trên
  feature và thông báo riêng cho stale/offline read. Retry dùng `VtButton` hiện
  có, không mở pairing dialog hay mutation khi read chưa thành công.
- Unit coverage thêm catalog error, stale/offline, retry thành công và empty
  catalog hợp lệ (**4/4**); Manager Web tổng **31 unit tests**, typecheck/lint/
  production build pass; Chromium preview E2E **8/8**, axe không có violation
  `serious`/`critical`. Không đổi pairing protocol, database, runtime provider,
  Wi-Fi/network hoặc physical device state.
- M2 vẫn còn các route role/history/assistant read-state và production
  accessibility/error matrix; slice này chỉ đóng gate device-list riêng.

## Conversation History resilience slice (2026-08-04)

- `ConversationHistoryFeature` hiện không coi failure của history hoặc retention
  policy là empty state. Hai read request phải thành công mới mở danh sách; nếu
  policy không đọc được, lịch sử bị khóa tạm thời để tránh hiển thị sai retention.
  State list gồm `loading`, `ready`, `empty`, `error`, `offline`; retry dùng
  generation guard để response cũ không ghi đè lần refresh mới.
- Detail read có state riêng `idle/loading/ready/error/offline`. Khi detail lỗi,
  conversation item/context vẫn được giữ, transcript không bị thay bằng dữ liệu
  cũ và operator có nút `Thử lại chi tiết`; focus chuyển về summary có thể đọc
  được và error dùng `role=alert`. Refresh list hủy detail request cũ.
- Unit coverage thêm list error, retention error, empty, list retry, detail error
  và detail retry (**6/6**); Manager Web tổng **37 unit tests**, typecheck/lint/
  production build pass; Chromium preview E2E **8/8**, gồm history keyboard flow
  và axe không có violation `serious`/`critical`.
- Mốc này chỉ thay đổi UI state/observability; không thay retention policy,
  transcript/audio storage, API schema, database, provider, Wi-Fi/network hoặc
  physical device state.

## Assistant Index resilience slice (2026-08-04)

- Dashboard `AssistantIndexFeature` hiện phân biệt `loading`, `ready`, `empty`,
  `error` và `offline`; lỗi `listAssistants()` không còn rơi xuống empty state.
  Error summary có retry, `role=alert`, focus heading `tabindex=-1` và `aria-busy`
  trên feature. Stale snapshot vẫn hiển thị banner riêng khi read thành công.
- Search refresh có generation guard để response cũ không ghi đè kết quả truy vấn
  mới; debounce timer được dọn khi component unmount. Create/pair dialog chỉ
  được dùng trên snapshot thành công hoặc empty state hợp lệ.
- Unit coverage thêm generic error, offline, retry và empty (**4/4**); Manager
  Web tổng **41 unit tests**, typecheck/lint/production build pass; Chromium
  preview E2E **8/8**, axe không có violation `serious`/`critical`.
- Mốc này chỉ thay đổi UI state/async ownership; không đổi assistant API,
  persistence, provider selection, auth, Wi-Fi/network hoặc physical device.

## Role Config resilience slice (2026-08-04)

- `RoleConfigFeature` hiện yêu cầu cả role config và voice catalog đọc thành công
  trước khi mở form. Read state phân biệt `loading`, `ready`, `error`, `offline`,
  có retry/focus summary; không còn hiển thị form một phần khi voice list lỗi.
- Save/publish được bọc bằng bounded async ownership; lỗi network/offline hoặc
  revision được hiển thị inline với `role=alert`, giữ nguyên draft và không
  silently discard thay đổi. Revision conflict vẫn đi qua dialog recovery hiện
  có; không thêm fallback provider hay đổi publish semantics.
- Unit coverage thêm config error, voice error, retry, offline save và publish
  error (**5/5**); Manager Web tổng **46 unit tests**, typecheck/lint/production
  build pass; Chromium preview E2E **8/8**, axe không có violation `serious`/
  `critical`.
- Mốc này chỉ thay đổi UI state/async error handling; không đổi role schema,
  prompt/personality data, provider selection, API/database, Wi-Fi/network hay
  physical device state.

## Model & Memory resilience slice (2026-08-04)

- `ModelMemoryFeature` hiện phân biệt `loading`, `ready`, `error`, `offline` khi
  đọc workspace provider/memory; failure không còn để UI trống hoặc giữ control
  cũ như thể selection còn xác nhận. Retry có generation guard và focus summary.
- Provider controls chỉ render sau khi workspace đọc thành công; selection vẫn
  một active kind, disabled intent/memory invariant và no-fallback behavior không
  đổi. Mutation/provider-unavailable/revision-conflict path hiện có vẫn được giữ.
- Unit coverage thêm load error, offline, retry và success (**4/4**); Manager Web
  tổng **50 unit tests**, typecheck/lint/production build pass; Chromium preview
  E2E **8/8**, axe không có violation `serious`/`critical`.
- Mốc này chỉ thay đổi UI read-state/async ownership; không đổi provider config,
  runtime registry, API/database, auth, Wi-Fi/network hay physical device state.

## OpenAPI semantic problem contract slice (2026-08-04)

- Manager API dùng một helper gửi lỗi thống nhất với media type
  `application/problem+json`; các nhánh auth, CSRF, precondition, ownership,
  machine bearer và internal error không còn trả JSON thường lệ khác media type.
  Payload vẫn backward-compatible (`code` bắt buộc, `detail` optional), không ghi
  secret, token hoặc stack trace.
- OpenAPI transform sinh representative example theo status và domain: login
  dùng `INVALID_CREDENTIALS`/`LOGIN_THROTTLED`, internal route dùng
  `MACHINE_UNAUTHORIZED`, runtime config dùng `NO_PUBLISHED_CONFIG`, còn
  conversation ingest công bố `413 HISTORY_LIMIT_EXCEEDED`. Các status chung
  `400/401/403/404/409/422/428/503` đều có example có `code` và `detail`.
- Bổ sung `413` cho `/internal/v1/conversations/turns`, vì store đã có typed
  `HISTORY_LIMIT_EXCEEDED`; artifact `openapi/manager-api.json` được export lại
  từ Fastify và có drift check.
- Các route có `:id` dùng resource-id schema chung; khi PostgreSQL adapter active,
  pre-validation kiểm tra UUID cho path và `assistantId` machine/user payload,
  nên input malformed trả `400 VALIDATION_ERROR` thay vì để PostgreSQL driver
  biến lỗi parse thành `500`. Fixture/memory adapter vẫn giữ opaque IDs; UUID hợp
  lệ nhưng không tồn tại vẫn trả `404 NOT_FOUND`.
- Evidence: memory run **16 passed, 8 skipped**; isolated PostgreSQL run trên
  database tạm của instance riêng **24/24 passed**, gồm malformed-ID regression;
  `npm run lint`, `npm run build`, `npm run openapi:check` và `git diff --check`
  pass. Semantic test kiểm tra runtime content type, stable code/detail và
  mapping representative cases. Database `veetee_vubq` đang phục vụ không bị
  test mutate; không đổi schema production, provider selection/fallback,
  firmware, Wi-Fi hoặc service exposure.

## Control-plane regression rerun after `09f0a0d` (2026-08-04)

- PostgreSQL Manager API dùng database riêng `veetee_vubq` trên loopback
  `55432`: **24/24 passed**; `npm run lint`, `npm run build` và
  `npm run openapi:check` đều pass.
- Manager Web dùng generated client hiện tại: typecheck, ESLint, **50 unit
  tests** và production build đều pass. Chromium/a11y evidence trước đó vẫn
  giữ nguyên; chưa coi nó là full production keyboard/WCAG gate.
- Browser regression hiện tại chạy Chromium qua `npm run test:e2e`: **8/8
  passed**, gồm keyboard history Enter/Space, mobile overflow, provider fault
  không fallback, revision conflict giữ draft và Axe core surfaces không có
  violation `serious`/`critical`.
- Voice Server presence/session cleanup đã được nạp ở process `18100`; API
  `18101` và Web `18181` không bị restart ngoài phạm vi cần thiết. Database,
  secret store, Wi-Fi và private Tailscale mapping không đổi.

## Role response schema hardening (2026-08-04)

- `GET/PATCH /api/v1/assistants/{id}/role-config` giờ công bố response schema
  có `locale`, `basePrompt`, personality/speech/pipeline policy, `admission`,
  tools và `memoryEnabled`; `additionalProperties: true` vẫn được giữ có chủ ý
  cho field schema-driven additive, không khóa future provider/policy extension.
- `admission.maxActiveTurns` và `retryAfterMs` có range trong response contract,
  đồng nhất với request schema và Manager Web control đã publish trước đó.
- Contract test kiểm tra cả hai operation có required fields và known policy
  properties; OpenAPI export/check và generated-client regeneration pass.
- Verification: Manager API PostgreSQL **25/25 tests**, lint/build và
  `openapi:check` pass; Manager Web typecheck/lint/**51 unit tests**/build và
  Chromium E2E **8/8** pass. Không đổi database schema, wire protocol, provider
  fallback policy, Wi‑Fi hoặc physical firmware state.
