# M1 implementation note

## Đã có trong slice hiện tại

- `DeviceMcpBridge` dùng JSON-RPC ID tăng dần, allow-list descriptor từ snapshot,
  timeout cấu hình và generation cancellation; unit suite server hiện **9 tests
  pass**.
- Pipeline nhận tool-call delta, assemble arguments, gọi MCP device và tiếp tục
  LLM theo giới hạn round trong config; không chọn provider khác khi lỗi.
- Test `test_tool_call_round_trip_streams_answer_after_device_result` đã chứng
  minh fragmented JSON arguments được ghép đủ, gọi đúng `device.led.set` một
  lần theo generation, rồi bắt đầu TTS từ round LLM kế tiếp. Đây là bridge/tool
  loop evidence; hardware driver thật vẫn chờ MCP firmware milestone.
- WebSocket integration hiện có compatibility flow cho `ws-v1-compat` và
  `ws-v2` (handshake, listen, Opus uplink, STT/TTS downlink) bên cạnh v3
  default; server suite **25 tests pass**. Đây là một nửa conformance matrix,
  chưa thay thế probe chéo với firmware/server tham chiếu thật.
- Realtime mode có server-side barge-in gate theo `bargeIn.minSpeechFrames`;
  button/local mute vẫn phải do firmware physical layer thực hiện.
- Turn cancellation đã có generation ownership guard: pipeline kiểm tra
  cancellation trước/sau mỗi TTS chunk và trước khi packetize; session xoá
  ownership trước khi await task cũ, đồng thời bỏ mọi control/audio frame mang
  `turn_id` cũ. Unit test `test_cancelled_turn_drops_queued_audio_packet` chứng
  minh packet không được phát sau barrier; integration test
  `test_realtime_barge_in_cancels_old_turn_before_new_audio` chứng minh speech
  frame trong lúc TTS đang nói tạo `tts.stop(reason=barge_in)` và không để
  binary stale lọt sang turn kế tiếp.
- Progress acknowledgement đã chạy theo snapshot (`deadlineMs`,
  `acknowledgementId`, `acknowledgements`); test fixture chứng minh ack được phát
  trước LLM answer chậm mà không chèn câu chữ trong code. PhoWhisper ingress dùng
  file-backed float32 spool thay vì giữ toàn bộ PCM chunks trong RAM.
- Registry đã có selection tùy chọn cho `veetee.intent.patterns` và
  `veetee.memory.session-window`: exit/cancel pattern phát event + abort có
  generation guard, còn memory giữ bounded JSON turn window theo từng WebSocket
  session. Cả rule/locale/action và giới hạn memory đều đi từ provider config.
- Silero VAD adapter đã sửa đúng context window của ONNX model (`512 + 64`
  samples ở 16 kHz), thay vì chạy cửa sổ rời khiến speech probability gần 0.
  Unit suite **22 tests pass**; model artifact local hash
  `1a153a22f4509e292a94e67d6f9b85e8deb25b4988682b7e174c65279d8788e3` nhận
  speech sample tiếng Anh/Việt sau khi có context. Trước candidate activation,
  production snapshot vẫn giữ energy VAD cho tới khi benchmark promotion gate
  được ghi nhận.
- Sau smoke test, owner-configured snapshot revision `36` đã chọn
  `veetee.vad.silero` với artifact local hash ở trên; Voice Server tạo registry
  mới, prewarm thành công, `activationFailures=0`, board reconnect lại và 5
  lượt manual E2E đều có downlink (TTFA `1247,4..1703,0 ms`, p50 `1485,1 ms`).
  Đây là candidate activation để đo tiếp, chưa phải M1 SLO/quality acceptance.
- Probe barge-in trên process `18100` dùng sample speech VieNeu đã resample
  16 kHz: server phát `tts.start`, nhận 8 speech frame khi đang nói, phát
  `tts.stop(reason=barge_in)`, và không có binary stale trước barrier. Đây là
  server/VAD evidence; firmware acoustic duplex/AEC và physical microphone vẫn
  chưa được acceptance.
- Pipeline giờ ghi stage metrics cho từng turn (`last_asr_finalize_ms`,
  `last_llm_first_token_ms`, `last_tts_start_ms`, `last_ttfa_ms`) từ mốc
  `listen/stop`; `/metrics` expose các giá trị này để dashboard phân biệt
  Operational TTFA với Lab/physical TTFA. Test fixture kiểm tra đủ bốn stage,
  không log transcript hay credential.
- Long-answer path đã bỏ việc giữ toàn bộ `answer_parts` trong RAM. Pipeline chỉ
  giữ excerpt tối đa theo `providers.memory.config.maxCharacters` trước khi gọi
  Memory provider; TTS/Opus vẫn stream theo segment và queue bounded. Regression
  `test_long_answer_keeps_memory_excerpt_bounded` xác nhận context không vượt
  giới hạn cấu hình trong khi vẫn phát binary audio.
- Smoke một lượt trên revision `36` sau restart trả `/metrics`:
  `last_asr_finalize_ms=681`, `last_llm_first_token_ms=1177`,
  `last_tts_start_ms=1180`, `last_ttfa_ms=1745` (ms từ server nhận
  `listen/stop`); `protocol_errors=0`, `active_connections=1`. Harness wall
  TTFA tương ứng `1804,9 ms`, chênh lệch là thời điểm gửi/nhận WebSocket và
  không được trộn hai định nghĩa trong SLO.
- Host-native Web proxy map `/api`, `/openapi.json`, `/veetee` qua target được
  cấu hình; private Tailscale Serve đang proxy UI trên hostname tailnet.

## WakeNet bring-up — model init đã pass, physical recognition còn mở

- Wake detector đã được thêm dưới abstraction `vt_wake_*`; application chỉ gọi
  init khi `CONFIG_VEETEE_WAKE_ENABLED` bật và có nhánh fail-safe giữ PTT
  (`veetee-firmware/main/main.c:607-618`). Idle capture không uplink audio cho tới
  khi detector phát hiện; event wake được chuyển thành `listen/start mode=auto`.
- Physical probe đầu tiên với model ESP-SR/WakeNet đã load được partition và
  manifest nhưng `iface->create()` panic `StoreProhibited`. Root cause được xác
  định là cấu hình firmware tắt hoàn toàn PSRAM (`CONFIG_SPIRAM`/Octal PSRAM),
  không phải phrase hay threshold. Sau khi bật Octal PSRAM 8 MiB và giữ lại
  reserve internal cho DMA, model 291 KiB được khởi tạo thành công.
- Build/flash cuối dùng model `wn9_computer_tts`; serial xác nhận
  `Found 8MB PSRAM device`, `Successfully load srmodels`, manifest phrase
  `Computer`, `veetee-wake: ready model=wn9_computer_tts rate=16000 chunk=512`,
  `ST7789 ready 240x280`, Wi-Fi profile từ NVS, IP `192.168.110.237`,
  `WebSocket v3 ready`, `server hello accepted; session ready`, và không có panic.
  Flash không chạy `erase-flash`, nên không thay đổi NVS/Wi-Fi.
- Đây là host/serial evidence cho boot và model-init. Chưa phát audio vào board ở
  lượt này; LCD, loa, mic, nút và tỷ lệ nhận dạng phrase vẫn cần physical check.

## Auto-turn ownership fix — endpoint frame và late audio

- Server auto mode trước đây có thể schedule `finish()` ngay sau khi VAD báo
  endpoint, trước khi `pipeline.ingest()` nhận frame cuối. Đã đổi thứ tự để ASR
  luôn nhận đủ frame kích hoạt endpoint (`veetee-server/src/veetee_server/app.py`).
- Khi turn manual/auto đã sang `thinking`/`speaking`, hoặc turn cũ đã hoàn tất và
  đang ở `listening`, server bỏ qua binary audio đến muộn thay vì append vào ASR
  đã finalize; counter `audio_frames_ignored` được tăng để quan sát.
- Firmware auto capture dừng ngay khi nhận `tts/start`, giữ cờ để `tts/stop` re-arm
  WakeNet đúng một lần. Như vậy microphone không upload ambient audio trong lúc
  server phát câu trả lời. Acoustic duplex/barge-in vẫn là gate riêng khi AFE/AEC
  được promotion; PTT interrupt không đổi.
- Regression test `test_auto_endpoint_ingests_last_frame_and_ignores_late_audio`
  chứng minh thứ tự `asr_accept → asr_finish`, một frame được nhận và frame đến
  muộn bị bỏ. Đây là host evidence, chưa thay thế physical audio acceptance.

## Flash verification after auto-turn fix (2026-08-04)

- Lần ghi ở baud `460800` bị USB/RTS reset khi đang ghi model khoảng 87%; không
  chạy `erase_flash`, NVS không bị chạm và board vẫn boot app cũ. Retry explicit ở
  baud `115200` hoàn tất, esptool verify hash pass cho bootloader, partition table,
  OTA metadata, model và app.
- Serial sau reset xác nhận firmware đúng commit `6dc99e9`, PSRAM 8 MiB,
  `ST7789 ready 240x280`, `Successfully load srmodels`,
  `veetee-wake: ready model=wn9_computer_tts rate=16000 chunk=512`, Wi-Fi profile
  từ NVS với IP `192.168.110.237`, `WebSocket v3 ready` và
  `server hello accepted; session ready`; không có panic.
- Đây chỉ là build/flash/serial evidence. Không phát audio test trong lượt này;
  wake recognition, loa, mic, PTT, AEC/noise và barge-in vẫn cần physical check.

Voice Server production-like runtime sau đó được restart bằng source của
`6dc99e9`, prewarm PhoWhisper/VieNeu hoàn tất, Manager poll ETag trả `304`, và
`GET http://127.0.0.1:18100/health/ready` giữ `revision=38`,
`activationFailures=0`, `activeConnections=1` (kết nối ESP32). Manager API
`18101` và Manager Web `18181` vẫn healthy; các listener `8000/8001/8081` thuộc
checkout khác nên không bị restart.

Host protocol fixture được mở rộng để kiểm tra golden bytes v1 raw Opus, v2 header
16 byte, v3 header 4 byte, timestamp/round-trip và các lỗi truncated/header/length/
oversized/capacity. CTest firmware vẫn pass `1/1`; đây là wire evidence bổ sung,
không thay thế cross-conformance với peer ngoài hoặc physical audio.

## Verification re-run sau mốc `55084db`

- Voice Server: `veetee-server/.venv/bin/pytest -q` → **26 passed**.
- Firmware host: CMake + CTest → **1/1 passed**; ESP-IDF binary đã build ở
  `6dc99e9` và đang chạy trên board.
- Manager API: `VEETEE_TEST_DATABASE_URL_FILE=../secrets/manager.database-url npm test`
  → **9/9 passed**, gồm PostgreSQL restart/revision/secret-history tests; `npm run
  lint` và `npm run build` pass.
- Manager Web: typecheck, ESLint, Vitest **12/12** và production build pass.
- Runtime tools: `python3 -m pytest -q tests` → **5 passed**; Voice/API/Web
  production-like health lần lượt ở `18100/18101/18181` đều pass.

## Chưa đạt M1 DoD

WakeNet recognition bằng audio thật, shared AFE/AEC/noise suppression, long-session
soak, Groq/VieNeu warm TTFA dưới target và physical MCP chưa được acceptance. Model
đã init ổn định; không dùng riêng log boot để tuyên bố wake word hay hội thoại đã
chạy end-to-end.

## First-speech watchdog — empty wake recovery (2026-08-04)

Audit runtime phát hiện `listen/start mode=auto` sau wake word sẽ giữ admission
lease vô hạn nếu không có speech: VAD chỉ endpoint khi đã đạt `minSpeechMs`, nên
`minSilenceMs` không thể tự giải phóng một lượt hoàn toàn im lặng. Đã bổ sung
policy additive `autoTurn` trong role snapshot và Manager UI/API.

- Watchdog sở hữu bởi `(session, turn_id, generation)`, chỉ chạy khi policy
  `enabled=true`; timeout 1.000–60.000 ms do owner publish.
- Hết hạn gửi `alert.code="NO_SPEECH_TIMEOUT"` với status/message/emotion từ config,
  release lease và về idle; không gọi ASR final, LLM hoặc TTS.
- Có regression cho silence release (`active_turns` về 0 và lượt kế tiếp được
  admit) và speech-before-deadline (watchdog bị hủy, lease vẫn sống).
- Đây là host/protocol evidence; physical wake → im lặng → firmware alert/re-arm
  vẫn cần chạy trên board sau khi publish policy vào runtime thật.

### Physical no-speech recovery (2026-08-04)

Sau khi publish assistant runtime revision `87` với `autoTurn.noSpeechTimeoutMs=5000`
và reload Voice Server, harness `wake_audio_test.py --allow-audio` chạy trên
ESP32 đang cắm `/dev/ttyACM0` đã pass:

- `wake_detected` → `wake start` → phát clip im lặng 6 giây.
- Firmware nhận `server alert code=NO_SPEECH_TIMEOUT` và `wake detector armed`.
- Voice health sau run: `ready`, `activeTurns=0`, `activationFailures=0`.
- Metrics sau lượt: `auto_no_speech_timeouts=1`, `protocol_errors=0`,
  `audio_frames_ignored=1`; late Opus frame sau alert bị drop có chủ đích, session
  không bị đóng.

Hai lượt trước khi late-frame fix cũng tái hiện alert/re-arm nhưng làm tăng
`protocol_errors` do frame đang nằm trong queue bị coi là violation. Regression
host và lượt physical cuối đã xác nhận race này được xử lý. Report redacted nằm
ở `/tmp/veetee-no-speech-late-frame-fix-20260804.json`, không đưa audio/raw serial
vào repository.

## Evidence / rollback

Mốc code trước bring-up đã push: `34a65b7`. Rollback về `ccc35c0` giữ firmware
scaffold và M0 server. Mốc bring-up PSRAM/WakeNet đã được commit và push tại
`253a4ba`; khi được phép phát audio, bổ sung corpus/benchmark/serial và physical
evidence ở đây; không ghi secret.

## Unattended wake gate — ưu tiên cho lượt physical test kế tiếp

- Quyết định vận hành: ưu tiên **on-device wake word** trước khi đánh giá các
  hạng mục acoustic khó hơn. Wake word loại bỏ phụ thuộc vào thao tác PTT khi
  chạy test từ xa, đồng thời giữ đúng policy idle không uplink microphone.
- Build đang chạy đã sẵn sàng cho gate này: model `wn9_computer_tts`, phrase
  `Computer`, 16 kHz/512-sample detector chunk, PSRAM 8 MiB; server readiness
  hiện `revision=39`, `activeConnections=1`, `activationFailures=0`.
- Khi chủ dự án cấp quyền phát audio, harness sẽ phát một clip wake sạch trước,
  chờ log `wake detected`/`wake start`, rồi mới phát utterance kiểm thử để đo
  `listen/start mode=auto` → ASR → Groq → VieNeu streaming → downlink Opus.
  Không phát clip hay bật microphone-to-speaker automation trước quyền đó.
- Gate pass cần evidence host + serial + audio timing; riêng false
  accept/false reject, loa/mic, AFE/AEC/noise và acoustic barge-in vẫn là
  physical acceptance, không được suy ra từ model-init hoặc unit test.

## Regression verification — current workspace

Sau mốc `200e0e2`, các lệnh được chạy lại theo đúng thư mục sở hữu; không dùng
pytest từ root để tránh thu nhầm test module ngoài `PYTHONPATH`:

- Voice Server: `cd veetee-server && .venv/bin/pytest -q tests` → **28 passed**.
- Firmware host: CMake build + CTest → **1/1 passed** (golden framing/state/wake
  host coverage hiện có).
- Manager API: `VEETEE_TEST_DATABASE_URL_FILE=../secrets/manager.database-url
  npm test` → **9/9 passed**, gồm PostgreSQL restart/revision/secret-history;
  `npm run lint` và `npm run build` pass.
- Manager Web: `npm run typecheck`, `npm run lint`, `npm test` → **12/12 unit
  tests**, và `npm run build` pass.
- Runtime tools: `cd tools/runtime && python3 -m pytest -q tests` → **5 passed**.
- Runtime readiness lúc ghi note: Voice `18100` revision `39`, API `18101`
  revision `39`, Web `18181`; Voice `activeConnections=1` và
  `activationFailures=0`.

Các kết quả trên chỉ xác nhận host/runtime regression. M0/M1 physical speaker,
mic/PTT và wake recognition vẫn giữ trạng thái pending cho đến khi có phép phát
audio và người dùng xác nhận phần nghe/nhìn/chạm.

## Firmware additive hello và presence acceptance (2026-08-04)

- Sau khi Manager/Voice mốc `b133e61` đã ready, firmware được build bằng ESP-IDF
  `6.0.2` và flash tại `115200` vào `/dev/ttyACM0`; lệnh không chạy
  `erase-flash`, nên không xóa NVS hoặc thay Wi-Fi profile.
- Serial sau reboot xác nhận app version `b133e61`, PSRAM `8MB`,
  `ST7789 ready 240x280`, model `wn9_computer_tts`, `veetee-wake: ready`, Wi-Fi
  profile đọc từ NVS, IP nội bộ hiện tại, `WebSocket v3 ready` và
  `server hello accepted; session ready`. Không có panic.
- Voice `/metrics` ghi một event presence được enqueue và gửi thành công;
  PostgreSQL `veetee_vubq` ghi nhận row unpaired mới với board/firmware metadata
  đã redact identity. Đây là physical serial + control-plane acceptance cho
  `device_info`, không phải acceptance của mic, loa, nút hay wake recognition.
- Không phát clip audio trong lượt này. Cổng unattended wake vẫn chờ chủ dự án
  cấp `--allow-audio`; khi được cấp, harness sẽ phát phrase đã cấu hình rồi mới
  phát utterance tiếng Việt.

Voice Server health hiện có thêm `lastActivationErrorType` để chẩn đoán lần
activation/poll lỗi mà không ghi detail có thể chứa path/secret. Lượt kiểm tra
health trên process production-like đang chạy ghi nhận `activationFailures=7`
nhưng counter không tăng trong các lần đọc liên tiếp và revision hiện tại vẫn
ready; đây là counter tích lũy từ các lần reload trước, không được coi là zero
failure. Source telemetry mới đã được test trên process fixture tạm ở `18102`;
production-like process `18100` chỉ nhận field này sau lần reload có kiểm soát.

## Unattended wake harness

- Đã thêm `tools/physical/wake_audio_test.py` và scenario mẫu
  `tools/physical/wake-test.example.json`. Harness kiểm tra health, mở
  `idf.py monitor --no-reset`, phát wake clip, chờ marker serial theo thứ tự,
  rồi mới phát utterance; player/monitor đều chạy bằng argv, không qua shell.
- Có safety gate bắt buộc `--allow-audio`; `--dry-run` chỉ resolve command/path và
  không mở serial hoặc audio. Unit test harness hiện **3/3 passed**.
- WAV/scenario local và report được ignore; không ghi clip, transcript, Wi-Fi
  credential hay Groq key vào repository. Physical run vẫn chưa được thực hiện
  trong note này vì chưa có quyền phát audio.

## Audio fixture đã chuẩn bị, chưa playback (2026-08-04)

- Dùng VieNeu v3 Turbo ONNX/int8 với voice `Minh Đức` để tạo hai WAV local bị
  ignore: `tools/physical/local-wake-computer.wav` (phrase preset `Computer`,
  mono 48 kHz, khoảng 1,04 s) và `tools/physical/local-utterance-vi.wav` (câu
  tiếng Việt, mono 48 kHz, khoảng 3,20 s).
- Chỉ kiểm tra file tồn tại và metadata bằng `ffprobe`; không gọi `pw-play`, không
  mở microphone và không gửi audio tới ESP32. Dry-run harness resolve đủ cả hai
  clip (`missingClips: []`).
- Lệnh physical đã sẵn sàng sau khi chủ dự án cấp quyền:

  ```bash
  source /home/vubq/.espressif/v6.0.2/esp-idf/export.sh
  python3 tools/physical/wake_audio_test.py \
    --scenario tools/physical/wake-test.example.json \
    --allow-audio --report /tmp/veetee-wake-test.json
  ```

## Provider no-fallback guard (2026-08-04)

- `ProviderRegistry` reject snapshot có `fallback`, `fallbackProviderId` hoặc
  `fallbackProviderIds` ngay trước activation; không silently bỏ qua cấu hình
  khiến operator tưởng có provider dự phòng.
- Regression inject active TTS provider fault, chờ typed
  `TTS_SYNTHESIS_FAILED`/alert và giữ secondary fake provider counter ở `0`.
  Voice Server test suite hiện **39 passed**.

## Audio codec lock split và unattended wake soak (2026-08-04)

- Root cause của warning `Opus capture encode failed` đã được cô lập bằng
  telemetry contract: firmware dùng chung một mutex cho Opus encoder và decoder;
  `playback_task` có thể giữ mutex trong lúc decode + I2S write, khiến
  `capture_task` timeout đúng lúc bắt đầu turn mới và bỏ frame đầu. Đây là lỗi
  tranh chấp timing, không phải `esp_opus_enc_process` tự lỗi.
- `veetee-firmware/main/main.c` đã tách lock domain thành
  `audio_encoder_lock` và `audio_decoder_lock`. Capture chỉ giữ encoder lock;
  playback và decoder reset chỉ giữ decoder lock. Encoder không bị reset giữa
  các lượt, còn decoder vẫn được reset khi `tts/start` để không phát audio cũ.
- `veetee-firmware/main/veetee_audio.c` ghi contract lúc init
  (`input_bytes`, `output_hint`, `frame_samples`) và ghi typed telemetry khi
  vendor API reject hoặc trả encoded output ngoài khoảng `1..capacity`; không ghi
  PCM, transcript hay credential.
- Build/flash ESP-IDF 6.0.2 ở baud `115200` đã verify hash pass, không chạy
  `erase-flash` và không thay NVS/Wi-Fi. Board chạy đúng image split-lock với
  PSRAM 8 MiB, WakeNet `wn9_computer_tts`, WebSocket v3 và server session ready.
- Smoke physical sau fix: **2/2 repetition**, đủ 8/8 event
  (`wake detected → wake start → state=speaking → wake detector armed`), không
  còn `Opus capture encode failed`, decoder error, panic, stack overflow hay
  queue backpressure quan sát được.
- Unattended audio soak dùng `tools/physical/wake_audio_test.py` với
  `--allow-audio`: **30/30 repetition, 120/120 event**, mỗi repetition đúng 4
  event, `errors=[]` trong `/tmp/veetee-wake-soak-30-split-lock.json`. Voice
  `/health/ready` sau soak vẫn `status=ready`, `activeTurns=0`,
  `lastActivationErrorType=null`; `activationFailures=1` là counter tích lũy
  lịch sử và không tăng trong test này.
- Kết quả này loại **encoder warm-up** khỏi blocker hiện tại. Nó chưa chứng minh
  AFE/AEC/noise suppression, acoustic barge-in, false accept/reject wake corpus,
  physical PTT/LCD/loa hoặc cross-conformance với peer ngoài; các gate đó vẫn
  giữ nguyên ở M1.

### Regression verification sau split-lock

- Firmware host CMake/CTest: **1/1 passed** (`protocol_state`).
- Physical harness tests + Python compile: **4 passed**.
- Voice Server: **54 passed**.
- Các kết quả trên là host/serial/harness evidence; người dùng vẫn cần xác nhận
  nghe tiếng từ loa, thấy trạng thái trên LCD và thao tác nút PTT riêng.

### Exact-image smoke sau commit `a573e08`

- Firmware được build lại với app version `a573e08` (`veetee-firmware.bin`
  `0x148500`, còn 67% app partition) và flash `/dev/ttyACM0` ở `115200`. Quy
  trình chỉ ghi bootloader, partition table, OTA metadata, model và app; không
  chạy `erase-flash`, không xoá NVS/Wi-Fi.
- Boot serial của đúng image xác nhận PSRAM 8 MiB, ST7789 `240x280`, startup
  chime, Opus encoder contract, WakeNet `wn9_computer_tts` phrase `Computer`
  threshold `65`, Wi-Fi profile từ NVS, IP nội bộ, WebSocket v3 và
  `server hello accepted; session ready`; không thấy panic/stack overflow.
- Harness có `--allow-audio` phát wake WAV và utterance tiếng Việt một lần:
  report `/tmp/veetee-wake-exact-a573e08.json` ghi **4/4 event**
  (`wake_detected`, `capture_started`, `assistant_speaking`, `wake_rearmed`).
  Wake detect ở `2277,5 ms`, `state=speaking` ở `7829,0 ms`, re-arm ở
  `30184,4 ms`; đây là wall timing của harness, không phải TTFA SLO.
- Voice metrics sau lượt: `protocol_errors=0`, `active_turns=0`,
  `last_ttfa_ms=1348`, `last_asr_finalize_ms=520`,
  `last_llm_first_token_ms=896`, `last_tts_start_ms=899`; health vẫn
  `status=ready`, `activeConnections=1`, `lastActivationErrorType=null`.
- Đây là bằng chứng board đã nhận audio và hoàn tất state/TTS path; nó không tự
  xác nhận âm lượng/chất lượng loa, nội dung LCD hoặc thao tác PTT bằng tay.

## PTT interrupt trong thinking (2026-08-04)

- Trước mốc này, nhấn PTT chỉ gửi `abort` khi thiết bị đang `speaking`; nếu
  Groq/ASR/intent đang ở `thinking`, nút bị bỏ qua cho tới khi TTS bắt đầu.
- `vt_state_is_interruptible()` hiện định nghĩa tập trạng thái dùng chung là
  `thinking` và `speaking` (`veetee-firmware/main/veetee_state.c`). `ptt_task`
  dùng predicate này cho cả nút vật lý và wake interrupt: dừng capture, gửi
  `abort(reason=button_interrupt|wake_word_detected)`, reset playback queue và
  áp dụng `VT_EVENT_ABORT` để tăng generation trước khi turn mới được nhận.
- Host CTest thêm coverage cho predicate: `idle/listening` không interrupt,
  `thinking/speaking` interrupt; toàn bộ firmware host test vẫn **1/1 passed**.
- Exact image `3d4cbc9` đã build (`0x148550`, còn 67% app partition), flash
  `115200` không erase/NVS và boot serial xác nhận app version đúng commit,
  PSRAM/LCD/WakeNet/Wi-Fi/WebSocket v3/server hello. Audio smoke report
  `/tmp/veetee-wake-exact-3d4cbc9.json` đạt **4/4 event**; Voice metrics sau
  lượt có `protocol_errors=0`, `active_turns=0`, `last_ttfa_ms=1316`.
- Đây là host/serial/state evidence; cần một lượt thao tác PTT thật trong lúc
  server `thinking` hoặc `speaking` để đo time-to-silence và xác nhận nút/loa
  trực tiếp. Không đánh dấu acoustic barge-in/AEC đã đạt từ thay đổi này.

## Realtime Lab sau mốc PTT interrupt (2026-08-04)

- Host-only probe chạy trên Voice Server đang phục vụ board, không mở sound
  device, microphone hoặc serial và không phát audio vào ESP32. Lệnh dùng WAV
  fixture tiếng Việt, 3 turn với 1 warm-up; report redact ở
  `/tmp/veetee-realtime-3d4cbc9.json`.
- Cả 3 turn đều có `tts.start`, binary downlink, `tts.stop`, không alert và
  không protocol error. TTFA lần lượt `1634,3`, `1377,5`, `1164,7 ms`; sau khi
  bỏ warm-up, warm p50 `1164,7 ms`, p95 `1377,5 ms`, **pass** gate `<1500 ms`.
- Voice `/metrics` sau probe vẫn `active_turns=0`, `protocol_errors=0`; health
  `status=ready` và board giữ một connection. Đây là evidence warm operational
  path, không thay thế 60-minute real-provider soak hay physical speaker/mic.

## Session cleanup regression và unattended-test readiness (2026-08-04)

- Host-only Realtime Lab sau lần reload Voice Server phát hiện lỗi cleanup khi
  một WebSocket đóng: `VoiceSession._report_presence()` là enqueue đồng bộ trả
  `bool`, nhưng nhánh `finally` lại `await` giá trị đó (`veetee-server/src/veetee_server/app.py:170-181`).
  Lỗi không làm mất toàn bộ audio response nhưng khiến AIOHTTP ghi exception ở
  mỗi disconnect, không chấp nhận được cho test unattended nhiều lượt.
- Đã bỏ `await` tại nhánh offline; presence reporter vẫn giao việc gửi cho
  background worker như thiết kế. Regression presence kiểm tra cả hai event
  `online/offline` và không còn `TypeError` (`veetee-server/tests/test_history_integration.py:97-135`).
- Verification: Voice Server **51/51 passed**, Ruff pass; process production-like
  `18100` đã reload từ source mới, health `ready`, revision `70`,
  `activationFailures=0`, `lastActivationErrorType=null`, và ESP32 tự reconnect
  lại (`activeConnections=1`).
- Realtime Lab vẫn ghi nhận TTFA first audio khoảng **1,33 s** ở một lượt host
  nhưng `tts.stop` có thể vượt timeout khi Groq trả câu dài và VieNeu còn stream
  phần còn lại. Đây là giới hạn timeout/long-answer cần đo riêng, không được
  đánh đồng với lỗi cleanup; physical gate sẽ chờ marker firmware speaking và
  re-arm thay vì chỉ dựa vào socket close.
- Wake word vẫn là gate physical đầu tiên: khi chủ dự án gửi quyền phát audio
  rõ ràng, chạy `tools/physical/wake_audio_test.py --allow-audio`; harness sẽ
  phát clip `Computer`, chờ `wake detected` → `wake start`, rồi phát utterance.
  Trước quyền đó không phát audio, không mở microphone và không reset/flash/NVS.
- Đây là host contract evidence cho policy không fallback; fault injection
  blue-green/quiesce-swap và provider unload/resource gate vẫn là promotion work
  riêng, chưa được đánh dấu đạt.

## Host-only Realtime Lab (2026-08-04)

- `tools/runtime/realtime_lab.py` là probe host-only: đọc WAV local, resample về
  16 kHz, encode Opus v3 rồi gửi vào Voice Server; không mở sound device, không
  mở serial và không phát audio vào ESP32. Report chỉ giữ event shape, số packet,
  TTFA và alert code đã redact.
- Runtime-tool suite sau khi thêm probe và regression timeout/encoder isolation:
  **16 passed** (`cd tools/runtime && python3 -m pytest -q tests`).
- Một lần probe thật ở Voice Server revision `65` đã nhận đủ handshake, `tts.start`
  và binary packet, không có protocol error. Kết quả ghi nhận warm p50 khoảng
  `1530,5 ms`, warm p95 khoảng `2045,5 ms`; một turn chưa nhận `tts.stop` trong
  timeout. Vì vậy probe này **không đạt** gate warm p95 ≤ `1500 ms` và không được
  dùng để tuyên bố M1/SLO pass. Đây là bằng chứng cần tối ưu warm TTFA/độ dài trả
  lời, không phải physical wake evidence.
- Tool đã tách Opus encoder theo từng session và chuyển timeout thành kết quả
  fail có cấu trúc thay vì traceback; câu trả lời dài vẫn được phép chạy tới
  timeout cấu hình, không bị cắt bởi product pipeline.

## Unattended wake hand-off

- Thứ tự physical gate vẫn là: readiness → phát clip wake `Computer` → chờ
  `wake detected`/`wake start` → phát utterance tiếng Việt → chờ `state=speaking`
  và `wake capture complete`. PTT giữ vai trò fallback khi cần phân biệt lỗi
  detector với lỗi pipeline.
- Harness chỉ mở player khi có cờ `--allow-audio`; không có cờ này thì dry-run
  không mở serial/player. Chưa có lượt playback nào trong mốc này.

## Deterministic session/long-answer soak coverage (2026-08-04)

- Thêm `veetee-server/tests/test_soak.py` với một WebSocket v3 session giữ nguyên
  trong **60 turn**. Mỗi turn đi qua Opus uplink, listen start/stop, pipeline fake
  ASR/LLM/TTS và downlink binary; test xác nhận 60 input frame, không alert,
  `tts.stop`, active connection về 0 và session set được dọn sau close.
- Cùng test stream một deterministic Unicode answer có checksum qua segmenter →
  TTS → packetizer với sink chỉ đếm frame/hash incremental. Memory provider chỉ
  nhận excerpt tối đa 128 ký tự; text/audio không được materialize thành một blob
  trong test oracle.
- Đây là deterministic host/session soak và long-answer ordering evidence, chạy
  nhanh để bắt stale ownership/coverage regression. Nó **chưa** thay thế 60 phút
  wall-clock với PhoWhisper/Groq/VieNeu thật, RSS/VRAM sampling, hoặc physical
  speaker/mic acceptance; các gate đó vẫn mở.
- Regression sau khi thêm slice: `cd veetee-server && .venv/bin/pytest -q tests`
  → **41 passed**; runtime tools giữ **16 passed**.

## Provider generation lease/cleanup (2026-08-04)

- Runtime config giờ cấp lease cho từng WebSocket session. Session giữ nguyên một
  `RuntimeView`/provider generation từ hello tới close; poll revision mới không
  trộn VAD/ASR/TTS của generation mới vào một turn đang chạy.
- Generation cũ chuyển sang retired: chỉ close khi lease cuối cùng release. Nếu
  candidate `prepare()` fail, candidate registry được close và old generation vẫn
  active; `ProviderRegistry.close()` idempotent, VieNeu engine có close hook,
  PhoWhisper/Silero drop model/session references sau drain.
- Regression `test_retired_generation_closes_only_after_last_session_lease`,
  `test_failed_candidate_is_closed_and_old_generation_stays_active` và provider
  close tests pass. Đây là lifecycle/ownership evidence host-only; chưa phải
  measured VRAM peak, blue-green/quiesce resource benchmark trên model thật.
- Full Voice Server suite sau mốc này: **44 passed**; `ruff` không nằm trong
  virtualenv hiện tại nên chưa có lint evidence riêng cho Python.

## Runtime reload evidence (2026-08-04)

- Sau commit `6ea1cda`, supervisor target `host-native-postgres-dev.json` đã được
  dừng/chạy lại bằng host-native process riêng. PostgreSQL vẫn là instance
  `127.0.0.1:55432`, database `veetee_vubq`; không chạm listener/route của
  checkout khác và không xóa NVS/Wi-Fi.
- Voice Server thực tế chạy source mới ở revision `66`: `/health/ready` trả
  `activationFailures=0`, `lastActivationErrorType=null`, `activeConnections=1`;
  Manager API `18101` và Web `18181` đều ready. Serial-presence reconnect đã
  gửi thành công, nhưng `audio_frames_in/out=0` trong lượt này vì chưa được phép
  phát audio.

## Runtime recovery trước unattended wake gate (2026-08-04)

- Lần khởi động supervisor transient đầu tiên thất bại trước khi hoàn tất dependency
  graph: user-systemd không kế thừa PATH của nvm nên `npm` không được resolve;
  PostgreSQL đã dừng cùng supervisor, không có dữ liệu bị xóa và không chạm vào
  port/network của checkout khác.
- Đã khởi động lại cùng manifest bằng PATH Node tuyệt đối ở lớp vận hành (không
  đưa path hoặc credential vào product config). Migration `veetee_manager` chạy
  thành công; các listener riêng của checkout mới là PostgreSQL `55432`, API
  `18101`, Voice `18100`, Web `18181`.
- Readiness sau recovery: API `revision=45`; Voice `revision=45`,
  `activeConnections=1`, `activationFailures=0`, `lastActivationErrorType=null`;
  Web trả HTML Vite bình thường. ESP32 đã reconnect qua WebSocket mà không cần
  flash hoặc thay đổi NVS/Wi-Fi.
- Bài học vận hành: khi chạy unattended qua user-systemd phải inject PATH Node
  vào service invocation; không coi PATH interactive là dependency runtime. Khi
  chuyển sang unit bền vững, unit phải giữ nguyên nguyên tắc host-native và
  không chứa secret.

## Quyết định thứ tự physical gate

- Ưu tiên gate **on-device wake word** trước khi đánh giá acoustic barge-in và
  long-session physical soak. Đây là lựa chọn vận hành, không thay đổi wire
  contract: khi detector báo hit, firmware phát `listen/start mode=auto` như
  một nguồn start tương đương PTT.
- Lý do: wake word cho phép harness chạy unattended khi không có người bấm nút,
  đồng thời giữ idle không uplink microphone; PTT vẫn là fallback để phân biệt
  lỗi detector với lỗi pipeline. Acoustic barge-in chỉ được bật sau khi AFE/AEC
  được xác nhận, tránh kết luận sai từ một nửa-duplex capture.
- Gate này vẫn bị khóa bởi policy phát audio. Chỉ sau khi chủ dự án cấp quyền,
  harness mới phát clip wake và utterance; trước đó chỉ được dùng host test,
  serial monitor `--no-reset` và health evidence.

## Regression sau recovery (2026-08-04)

- Voice Server: `cd veetee-server && .venv/bin/pytest -q tests` → **28 passed**.
- Firmware ESP-IDF: source ESP-IDF `v6.0.2`, `idf.py build` → tạo
  `build/veetee-firmware.bin`, app còn **67%** dung lượng partition; không flash
  ở lượt này.
- Firmware host: `cmake --build host-tests/build-current` + CTest → **1/1 passed**.
- Manager API: PostgreSQL test suite → **10/10 passed**; `lint` và `build` pass.
- Manager Web: typecheck/lint/Vitest → **12/12 passed**; production build pass.
- Runtime tools: `python3 -m pytest -q tests` → **5 passed**.
- Runtime đang giữ listener `55432/18100/18101/18181`; health Voice/API đều 200,
  Voice `activeConnections=1`, `activationFailures=0`. Không có audio playback,
  reset, erase NVS hoặc thay đổi kết nối mạng trong lượt regression này.

## Supervisor PATH hardening (2026-08-04)

- Đã sửa `tools/runtime/veetee_runtime.py` để mỗi service nhận một bản sao môi
  trường có Node bin được resolve theo thứ tự: `VEETEE_NODE_BIN`, npm hiện có
  trong PATH, rồi nvm dưới `$HOME/.nvm/versions/node/*/bin` theo semantic version.
  Cách này xử lý user-systemd không load shell profile mà không cần shell command,
  không ghi path/credential vào product config và không ảnh hưởng Python/firmware.
- Regression runtime tăng từ 5 lên **7 passed**; probe cô lập với
  `HOME=/home/vubq PATH=/usr/bin` resolve đúng
  `/home/vubq/.nvm/versions/node/v24.18.0/bin`.
- Stack đang chạy không bị restart trong patch này: Voice/API/Web/PostgreSQL vẫn
  giữ listener và ESP32 connection; không phát audio và không thay đổi mạng.

## Handshake validation hardening (2026-08-04)

- Voice Server giờ xử lý lỗi trong `hello` ngay cả trước steady-state message
  loop: handshake malformed đóng WebSocket với code `1002`, tăng
  `protocol_errors` và không để exception rơi thành lỗi HTTP/AIOHTTP không xác
  định.
- Parser kiểm tra rõ `version` là integer (không nhận boolean), `transport` là
  `websocket`, và `audio_params` gồm integer `channels=1`, `sample_rate=16000`,
  `frame_duration=60`. Điều này giữ đúng wire contract và tránh `int(...)` nuốt
  dữ liệu kiểu sai.
- Thêm ba regression cases cho transport sai, sample rate dạng string và version
  boolean; test riêng `test_app.py` **14 passed**, toàn bộ Voice Server **31
  passed**. Không phát audio và không restart runtime production-like.

## Session identity header gate (2026-08-04)

- WebSocket server giờ yêu cầu `Client-Id` cho profile product `ws-v2`/`ws-v3`,
  giữ `ws-v1-compat` cho peer cũ có thể thiếu header. `Device-Id` và
  `Client-Id` được bind vào cùng session; header rỗng/quá dài trả HTTP `400`
  trước khi upgrade.
- Firmware hiện đã gửi cả hai header; các test server được cập nhật để phản ánh
  wire contract thay vì vô tình chứng minh một handshake thiếu identity.
- Thêm regression cho v3 thiếu `Client-Id`; Voice Server toàn bộ suite hiện
  **32 passed**. Chưa flash firmware vì source firmware không đổi.

## Firmware server-hello validation (2026-08-04)

- Firmware transport giờ chỉ chuyển state `ready` sau khi server `hello` hợp lệ:
  version phải khớp profile, `transport=websocket`, session ID không rỗng/vượt
  buffer, audio phải là Opus mono với sample rate downlink và frame duration
  đúng config. Hello sai bị đóng kết nối và không được chuyển tiếp thành event
  `server hello accepted` giả.
- Binary downlink đến trước handshake được drop thay vì đưa vào playback queue.
  Đây là fail-closed ở firmware, bổ sung cho server handshake gate; không đổi
  layout v1/v2/v3 hoặc message semantics hợp lệ.
- `idf.py build` với ESP-IDF **6.0.2** pass; app `0x1487c0`, còn **67%** app
  partition. Firmware host CTest **1/1** pass. Chỉ build, không flash/reset và
  không phát audio trong lượt này.

## Wake gate readiness rerun (2026-08-04)

- Quyết định vận hành vẫn là **wake word on-device trước PTT-independent
  physical test**. Firmware build hiện chọn model từ config/manifest; detector
  chạy local ở 16 kHz, hit một lần rồi phát `listen/start mode=auto`, dừng uplink
  khi server phát TTS và arm lại sau `tts.stop`.
- `python3 tools/physical/wake_audio_test.py --dry-run` pass: command monitor có
  `--no-reset`, player dùng argv với clip path local; không mở serial, không chạy
  player và không phát audio. Unit harness **3/3 passed**.
- Firmware host CMake/CTest **1/1 passed**; ESP-IDF 6.0.2 `idf.py build` pass,
  binary app `0x1487c0` và còn 67% partition. Health Voice/API/Web đều ready;
  không flash, reset, erase NVS hay đổi Wi-Fi trong lượt này.
- Physical acceptance vẫn pending: cần quyền `--allow-audio` để phát clip wake
  rồi utterance; harness sẽ chờ serial `wake detected` → `wake start` →
  `state=speaking` → `wake capture complete` và ghi report timing không chứa
  transcript/credential.

## Groq connection-pool latency gate (2026-08-04)

- Groq LLM provider trước đây tạo một `httpx.AsyncClient` mới trong mỗi lượt;
  điều đó bỏ qua TCP/TLS keep-alive và làm TTFA phụ thuộc vào connection setup.
- Provider hiện tạo một HTTP pool theo generation, tái sử dụng giữa các session
  trong cùng snapshot và đóng pool qua lifecycle `ProviderRegistry.close()` sau
  khi lease cuối của generation cũ được giải phóng. `maxConnections`,
  `maxKeepaliveConnections`, `keepaliveExpirySeconds` và `timeoutSeconds` là
  config fields trong provider catalog; không có fallback key/provider.
- Unit test provider xác nhận hai stream chỉ tạo một client, giữ đúng pool limits
  và gọi `aclose()` một lần khi provider đóng. Voice Server suite sau thay đổi:
  **45 passed**; Manager API test/lint/OpenAPI check: **15 passed, 7 skipped**,
  TypeScript và artifact OpenAPI đều pass.
- Host-only Realtime Lab tại Voice revision `66` chạy 4 turn (1 warm-up), không
  có protocol error/alert và mọi turn có `tts.start`/binary/`tts.stop`:
  `warmP50=1260.2 ms`, `warmP95=1325.2 ms`, dưới gate `1500 ms`.
  Đây là host network/provider evidence; không thay thế physical wake, loa/mic,
  AEC hoặc acoustic barge-in acceptance. Report đầy đủ nằm ngoài repository để
  không lưu transcript, audio hay credential.

## Runtime reload evidence for pool (2026-08-04)

- Voice process đã được reload có kiểm soát từ source commit `5db5509`; API/Web/
  PostgreSQL không restart, ESP32 tự reconnect và `/health/ready` trở lại
  `revision=66`, `activationFailures=0`, `activeConnections=1`.
- Lab chạy lại trên process đã nạp code mới bằng WAV wake fixture ở chế độ
  host-only (3 turn, 1 warm-up), tất cả turn có `tts.start`, binary và `tts.stop`,
  không có protocol error/alert: `warmP50=1155.3 ms`, `warmP95=1362.8 ms`, pass
  gate `<1500 ms`.
- Không dùng `pw-play`, không mở microphone/serial và không phát audio tới board;
  đây chỉ là bằng chứng network/provider sau reload. Wake recognition, loa/mic,
  LCD, PTT và acoustic barge-in vẫn pending physical permission/acceptance.

## MCP bridge response hardening (2026-08-04)

- `DeviceMcpBridge.resolve()` giờ chỉ nhận JSON-RPC `2.0` với ID nguyên dương
  trong signed 32-bit range; boolean, zero, âm, ID quá lớn hoặc version khác bị
  bỏ qua mà không đánh thức pending call sai.
- `tools/call` dọn pending entry trong mọi đường timeout, cancellation, send
  failure và completion. Lỗi gửi xuống WebSocket được chuẩn hóa thành
  `TOOL_TRANSPORT_FAILED` có thể retry theo policy, nhưng không gọi provider/tool
  thứ hai và không replay mutation.
- Regression MCP gồm invalid envelope/ID và send failure cleanup; toàn bộ Voice
  Server suite sau thay đổi: **47 passed**. Đây là conformance/ownership evidence
  ở server; hardware MCP thật vẫn deferred cho đến khi có schematic và descriptor
  asset đã xác minh, không đoán GPIO hoặc quảng bá tool không tồn tại.

## Manager snapshot client resilience (2026-08-04)

- Runtime config poller trước đây mở/đóng một `httpx.AsyncClient` ở mỗi chu kỳ;
  client mới làm mất keep-alive và một disconnect thoáng qua bị ghi thành
  `activationFailures`. Poller hiện giữ pool generation-independent có
  `maxConnections=2`, một keep-alive slot và expiry 30 giây; lỗi `ConnectError`
  được retry đúng một lần sau 50 ms, sau đó giữ last-known-good snapshot như cũ.
- Khi poll kế tiếp đọc thành công snapshot không đổi (`304`/same checksum),
  runtime xoá `lastActivationErrorType` cũ nhưng giữ `activationFailures` dạng
  cumulative counter. Health vì vậy phân biệt được lỗi transient đã hồi phục
  với lỗi đang tiếp diễn; old generation vẫn không bị thay hoặc fallback.
- `RuntimeConfigManager.stop()` đóng pool đúng một lần; test xác nhận retry hữu
  hạn, reuse cùng client và cleanup. Voice Server suite sau thay đổi:
  **48 passed**.
- Đây là transport/runtime resilience evidence; không làm mất cảnh báo khi API
  thực sự không reachable, không đổi provider snapshot và không dùng fallback
  provider/key.

## Runtime pool reload evidence (2026-08-04)

- Voice process đã reload từ source có snapshot-client pool; board reconnect lại,
  `/health/ready` giữ `revision=66`, `activeConnections=1`,
  `activationFailures=0`, `lastActivationErrorType=null` sau các chu kỳ poll.
- Host-only Lab sau reload chạy 2 turn (1 warm-up), không protocol error/alert,
  có đầy đủ `tts.start`, binary và `tts.stop`: warm TTFA `p50=p95=1211.5 ms`,
  pass gate `<1500 ms`. Không phát audio và không mở microphone/serial.

## Wake owner hardening trước unattended audio (2026-08-04)

- Trước thay đổi này, `vt_wake_feed()` chạy trong `capture_task` nhưng
  `vt_wake_arm()` có thể bị gọi trực tiếp từ callback WebSocket và
  `network_task`. Đây là race ownership đúng tại AFE/WakeNet boundary, trái với
  nguyên tắc single fetch-owner trong [ADR-005](../ADR/ADR-005-on-device-wake-word.md).
- Firmware hiện dùng hai queue bounded: `wake_event_queue` chuyển detection từ
  capture task sang PTT/state owner; `wake_command_queue` chuyển yêu cầu re-arm
  từ transport/network sang capture task. Chỉ `capture_task` gọi `vt_wake_arm`,
  nên model state/input ring không còn bị arm/clean đồng thời với detect.
- Capture task không feed detector trong lúc đang upload một lượt capture; detector
  vẫn có thể nghe ở idle và lúc speaker phát để giữ đường wake interrupt, sau đó
  re-arm ở safe boundary. Queue đầy chỉ tạo warning/drop metric, không block
  network callback.
- Host firmware CTest và ESP-IDF build phải pass trước khi flash. Lượt này không
  phát audio; wake recognition, mic, loa, PTT và AEC vẫn cần owner physical gate.

## Flash evidence cho wake owner hardening (2026-08-04)

- Commit `924d216` đã được push lên `origin/master`. ESP-IDF 6.0.2 build lại từ
  working tree sạch, app image `0x1489b0`, còn 67% app partition; host CTest
  `1/1` pass.
- Flash bằng `idf.py -p /dev/ttyACM0 -b 115200 flash` hoàn tất và esptool verify
  hash pass cho bootloader, partition table, OTA metadata, model và app. Không
  chạy `erase_flash`; Wi-Fi/NVS không bị xoá hay provision lại.
- Serial sau reset xác nhận `App version: 924d216`, `Found 8MB PSRAM device`,
  `ST7789 ready 240x280`, `Successfully load srmodels`,
  `veetee-wake: ready model=wn9_computer_tts rate=16000 chunk=512`,
  `WiFi station profile loaded from NVS`, IP LAN hiện tại, `WebSocket v3 ready`,
  `state=listening` và `server hello accepted; session ready`; không có panic.
- Đây là build/flash/serial evidence cho ownership hardening. Không phát clip,
  không mở microphone/audio player; wake recognition, loa/mic, PTT, AEC và
  barge-in vẫn chưa được đánh dấu physical acceptance.

## Post-flash host-only realtime regression (2026-08-04)

- Sau khi board reconnect ở commit `924d216`, Voice Server/API/Web vẫn ready
  (`revision=67`, `activeConnections=1`, `activationFailures=0`). Realtime Lab
  host-only chạy 3 turn (1 warm-up) bằng WAV fixture, không mở sound device,
  serial hoặc microphone ESP32.
- Mọi turn có `tts.start`, binary downlink và `tts.stop`, không có
  `protocol_error`/alert. Warm TTFA: `p50=1230.2 ms`, `p95=1377.9 ms`, pass gate
  `<1500 ms`. Report đầy đủ nằm ngoài repository tại
  `/tmp/veetee-realtime-after-wake-owner.json` và không chứa transcript/secret.

## MCP cross-conformance ordering (2026-08-04)

- Peer firmware MCP xử lý `initialize` rồi mới xử lý `tools/list`, đồng thời
  nhận cursor cho danh sách phân trang (`references/xiaozhi-esp32/main/mcp_server.cc:384-409`).
  Response của peer được bọc trong outer `type=mcp` với `session_id`
  (`references/xiaozhi-esp32/main/protocols/protocol.cc:94-97`). Peer server cũng
  gửi `initialize`, chờ result rồi mới gửi `tools/list`/cursor tiếp theo
  (`references/xiaozhi-esp32-server/main/xiaozhi-server/core/providers/tools/device_mcp/mcp_handler.py:238-293`).
- Bridge Veetee trước đây fire-and-forget cả hai discovery request. Hiện
  `initialize()`/`list_tools()` giữ pending request, chờ response theo đúng thứ
  tự, xử lý `nextCursor`, và `cancel_all()` khi session đóng. Discovery chạy sau
  khi hello đã được gửi và receive loop đã hoạt động; không block handshake.
- Integration fixture `test_reference_mcp_peer_gets_ordered_initialize_then_tools_list`
  mô phỏng envelope response của peer và assert không có `tools/list` trước
  initialize result. Unit suite cũng kiểm tra cursor pagination, ID monotonic,
  cancellation và send-failure cleanup. Voice Server suite sau thay đổi:
  **50 passed**.
- Đây là wire/compatibility evidence host-only, chưa phải cross-conformance
  physical với binary peer thật. Snapshot server tham chiếu không khởi động được
  trong môi trường hiện tại vì các dependency runtime riêng (`torch`,
  `opuslib_next`, `silero_vad`, `funasr`) chưa có; không cài hoặc sửa repo
  `references/`. Firmware MCP hardware descriptor vẫn deferred vì chưa có BOM/
  schematic đầy đủ.

## MCP runtime reload và realtime regression (2026-08-04)

- Chỉ Voice Server của checkout này được reload từ source mới; Manager API/Web và
  PostgreSQL `veetee_vubq` giữ nguyên. Health sau reload: Voice revision `68`,
  `activationFailures=0`, board reconnect `activeConnections=1`.
- Host-only Realtime Lab 3 turn (1 warm-up) vẫn có `tts.start`, binary và
  `tts.stop`, không protocol error/alert: warm TTFA `p50=1163.2 ms`,
  `p95=1368.0 ms`, pass `<1500 ms`. Không phát audio, không mở serial/microphone.

## Unattended wake preflight hand-off (2026-08-04)

- Thứ tự gate được giữ cố định: **on-device WakeNet → capture auto → pipeline
  realtime**. Đây là bước physical kế tiếp trước acoustic barge-in; PTT vẫn là
  đường fallback để tách lỗi detector khỏi lỗi audio/pipeline. Quyết định này
  không đổi wire contract hoặc biến server-side wake thành product default.
- Voice Server và Manager API tại thời điểm preflight đều ready ở revision `69`,
  board connection vẫn được giữ nguyên. `pw-play` có mặt và PipeWire có default
  sink/source; không mở source, không phát clip và không thay đổi Wi-Fi/network.
- Hai fixture local bị ignore được kiểm tra metadata: wake `Computer` mono PCM
  48 kHz/1.04 s; utterance tiếng Việt mono PCM 48 kHz/3.20 s. Firmware WakeNet
  contract hiện tại là 16 kHz/512-sample chunks; conversion vào detector thuộc
  audio path của firmware và sẽ được xác nhận bằng serial/audio timing ở gate vật lý.
- `python3 tools/physical/wake_audio_test.py --dry-run` resolve đúng monitor
  `idf.py ... monitor --no-reset` trên `/dev/ttyACM0`, player `pw-play` và thứ
  tự marker `wake detected` → `wake start` → `state=speaking` →
  `wake capture complete`; harness test **3/3 passed**.
- Physical gate vẫn **pending owner permission**. Khi được cấp quyền phát audio,
  chỉ chạy lệnh trong `tools/physical/README.md`; harness tự phát wake clip,
  chờ marker rồi phát utterance, không cần người ở cạnh để bấm PTT. Report chỉ
  ghi timing/event shape, không ghi transcript, raw audio, credential hoặc Groq
  key.

## Runtime health recovery signal (2026-08-04)

- `RuntimeConfigManager.refresh_now()` giờ xoá `lastActivationErrorType` sau một
  lần đọc thành công snapshot không đổi (`304` hoặc cùng checksum), nhưng giữ
  `activationFailures` là counter tích lũy. Health không còn giữ lỗi
  `ConnectError` cũ sau khi Manager API đã reachable trở lại; old generation,
  provider selection và no-fallback semantics không đổi.
- Regression test kiểm tra counter không reset và error type được clear. Voice
  Server suite hiện **51/51 passed**, Ruff pass. Chưa restart process đang chạy
  trong lượt source-only này; health hiện tại có thể còn hiển thị counter/error
  của process cũ cho đến lần restart có chủ ý.

## Full host regression after `09f0a0d` (2026-08-04)

- Firmware host protocol/state: CMake + CTest **1/1 passed**; ESP-IDF 6.0.2
  `idf.py build` pass, image `0x1489b0`, còn 67% app partition. Đây chỉ là
  build evidence; không flash/reset trong lượt này.
- Voice Server: `cd veetee-server && .venv/bin/pytest -q tests` → **51 passed**;
  `uv run ruff check src tests` pass. Runtime tools: **16 passed**.
- Runtime sau mốc cleanup vẫn giữ listener riêng `55432/18100/18101/18181`;
  Voice/API/Web health đều ready, Voice `activationFailures=0`,
  `lastActivationErrorType=null`, `activeConnections=1`. Không thay đổi Wi-Fi,
  route máy tính, NVS hoặc provider fallback semantics.

## Terminal TTS error closes speaking phase (2026-08-04)

- Trước mốc này, nếu TTS lỗi sau khi đã gửi `tts/start` và phát một phần audio,
  pipeline chỉ gửi `alert`; firmware có thể giữ phase `speaking` và không nhận
  được tín hiệu để re-arm WakeNet.
- `TurnPipeline` giờ quản lý cờ `tts_started`/`tts_stop_sent`, gửi `alert` rồi
  đúng một `tts/stop` với `reason=<typed provider code>` cho cả lỗi terminal;
  đường hoàn tất bình thường cũng đi qua cùng helper để tránh stop trùng.
  Không có provider fallback và không đổi binary framing.
- Regression mới mô phỏng TTS thành công ở segment đầu rồi lỗi ở segment thứ hai:
  xác nhận có binary đầu, alert `TTS_SYNTHESIS_FAILED`, đúng một stop cùng reason
  và secondary provider counter bằng `0`. Voice Server suite hiện **52 passed**;
  Ruff pass. Đây là host protocol evidence; wake re-arm, loa và acoustic path
  thật vẫn cần physical test có quyền phát audio.

## Runtime reload after terminal-stop fix (2026-08-04)

- Voice Server target process `18100` đã được graceful reload từ source commit
  `d26a8be`; các process/API `18101`, Web `18181`, PostgreSQL `55432` và
  checkout ngoài phạm vi không bị restart.
- Prewarm VieNeu/PhoWhisper hoàn tất; `/health/ready` trả `revision=71`,
  `activationFailures=0`, `lastActivationErrorType=null`. ESP32 tự reconnect,
  presence POST trả `202 Accepted`, `activeConnections=1`.
- Không flash, không reset/erase NVS và không phát audio trong lượt reload này;
  physical wake/mic/loa/PTT vẫn chờ owner permission.

## Session admission/handover slice (2026-08-04)

- Audit server cho thấy tài liệu đã yêu cầu một active session lease nhưng
  `VoiceApplication` trước đây chỉ đếm `active_connections`; hai WebSocket cùng
  `Device-Id` vẫn có thể cùng điều khiển một speaker.
- Đã thêm admission map theo `Device-Id` sau client hello validation. Connection
  mới thay lease cũ; server abort turn cũ, gửi `tts/stop(reason=session_replaced)`
  và đóng socket cũ bằng close `4001/session_replaced`. Server chỉ trả hello cho
  connection mới sau khi cleanup cũ hoàn tất; late cleanup không xóa lease mới.
- Contract additive được ghi trong [ADR-018](../ADR/ADR-018-single-device-session-admission.md)
  và [protocol spec §3.4](../03-protocol-spec.md). Không đổi binary framing,
  provider selection/fallback hoặc firmware code.
- Regression `test_duplicate_device_hello_handover_closes_old_session` pass:
  old session nhận stop + close 4001, new session nhận hello và `active_connections`
  còn một. Đây là host WebSocket evidence; firmware reconnect, loa và WakeNet
  re-arm vẫn cần physical audio gate.

## Bounded turn resource admission (2026-08-04)

- Gap tiếp theo của Q-007 là nhiều device khác nhau vẫn có thể tạo turn song
  song. Đã thêm policy additive `admission.maxActiveTurns/retryAfterMs` vào
  runtime snapshot; fixture M0 dùng `1/250` và Manager API giữ field qua
  draft/publish/ETag.
- `VoiceApplication` reserve turn lease trước khi tạo pipeline. Khi đầy, session
  không bị đóng; server gửi `alert.code=SERVER_BUSY` + `retry_after_ms`, không
  gọi provider và firmware alert path sẽ abort/re-arm. Lease release idempotent
  sau abort, error, intent exit, disconnect hoặc normal completion.
- Regression `test_turn_admission_rejects_second_device_then_releases_after_abort`
  chứng minh device thứ hai bị từ chối đúng typed alert, retry sau abort thành
  công và `active_turns` về zero. Đây là host evidence; concurrency/VRAM measured
  promotion và 60 phút soak vẫn mở.

## Regression/runtime evidence sau admission slice (2026-08-04)

- Voice Server: `cd veetee-server && .venv/bin/pytest -q tests` → **53 passed**;
  `uv run ruff check src tests` pass. Firmware host CTest **1/1 passed** và
  ESP-IDF 6.0.2 build pass, app image `0x1489b0`, còn 67% app partition.
- Manager API PostgreSQL: **24/24 passed**, TypeScript lint/build pass. Manager
  Web typecheck/lint, Vitest **50/50** và production build pass. Runtime tools
  **16 passed**; wake harness **3/3 passed** và dry-run không mở serial/player.
- Voice process `18100` đã được restart riêng từ source mới (không flash/reset
  board); readiness revision `72`, `activationFailures=0`,
  `lastActivationErrorType=null`, ESP32 reconnect lại `activeConnections=1`.
- Realtime Lab host-only sau reload (không mở sound device/ESP32 microphone): 3
  turn gồm 1 warm-up, đủ `tts.start`, binary và `tts.stop`, không protocol error;
  warm TTFA p50 `1191.9 ms`, p95 `1200.3 ms`, pass gate `<1500 ms`. Report đã
  redact nằm ngoài repository tại `/tmp/veetee-realtime-after-admission.json`.
- Đây vẫn là host/runtime evidence. Wake recognition, PTT, LCD/loa/mic, AEC và
  barge-in vật lý chưa được đánh dấu đạt vì chưa có quyền phát audio.

## Manager Web admission controls (2026-08-04)

- Manager Web đã expose `admission.maxActiveTurns` và `admission.retryAfterMs`
  trong `RoleConfigFeature` bằng `VtInput`; không dùng browser-default input và
  không cần sửa `.env` để thay policy. Validation UI giữ đúng giới hạn API
  (1–8 lượt, 100–10.000 ms), payload HTTP và mock gateway cùng một contract.
- OpenAPI đã regenerate từ Fastify route schema; generated client parse/publish
  policy qua ETag. Unit test kiểm tra field hiển thị, dirty state và save draft
  giữ nguyên hai giá trị admission.
- Verification sau patch: Manager Web typecheck/lint, production build và
  **51/51 unit tests** pass; Manager API **24/24 tests**, lint/build và
  `openapi:check` pass. Đây là control-plane evidence; chưa mở concurrency vượt
  baseline `1` nếu chưa có benchmark VRAM/RAM.
- E2E Chromium **8/8 pass** sau khi sửa payload save để shallow-copy cả
  `admission` nested object; nếu truyền Vue reactive proxy thẳng vào mock gateway,
  `structuredClone` sẽ ném `DataCloneError` và UI báo nhầm lỗi kết nối. Regression
  này bảo đảm policy mới không làm hỏng đường save voice/prompt/conflict hiện có.

## Runtime reload sau mốc `9f14e9c` (2026-08-04)

- Chỉ Voice Server checkout mới trên `18100` được graceful reload từ source đã
  push; Manager API `18101`, Web `18181`, PostgreSQL `55432`, listener cũ và
  Wi‑Fi/NVS không bị thay đổi. Board tự reconnect và presence trả `202 Accepted`.
- `/health/ready`: `revision=74`, `activeConnections=1`, `activeTurns=0`,
  `maxActiveTurns=1`, `activationFailures=0`, `lastActivationErrorType=null`.
- Host-only Realtime Lab 3 turn (1 warm-up), không mở sound device/serial và
  không phát vào ESP32: mọi turn có `tts.start`, binary và `tts.stop`, không
  `protocol_error`; warm TTFA p50 **1253,7 ms**, p95 **1319,0 ms**, pass gate
  `<1500 ms`. Report redact ở `/tmp/veetee-realtime-after-9f14e9c.json`.

## Physical unattended wake and audio regression (2026-08-04)

- Owner đã cấp quyền phát audio. Chỉ checkout này được flash; không đổi Wi-Fi,
  NetworkManager, route máy tính hoặc NVS. Fixture wake local trước đó không
  khớp model đã chọn, nên đã tạo lại file ignored `tools/physical/local-wake-
  computer.wav` bằng câu tiếng Anh đúng model `Computer`; không commit audio,
  transcript hoặc report.
- Lượt preflight với clip cũ vẫn có mức mic (`peak=10370`, `mean_abs=2953`,
  `wake_ready=1`) nhưng không detect. Sau khi dùng clip đúng, threshold `90`
  (model log `0.900000`) không detect; threshold `65` khớp calibration trong
  manifest (`0.648–0.650`) và detect ổn định. Kconfig product default đã đổi
  thành `65`, vẫn giữ `mode=90` và chọn model từ asset/config.
- Lượt đầu sau detection phát hiện `***ERROR*** A stack overflow in task
  vt_capture`. Đã loại VLA I2S và buffer capture khỏi task stack bằng buffer
  pre-allocated, thêm guard `bytes_read`/`sample_capacity`, rồi tăng stack theo
  số đo. Lượt tiếp theo lộ tương tự ở `vt_playback`; packet decode được giữ ở
  static storage của single-owner task và playback stack tăng lên `16 KiB`.
- Sau các lượt stack, một frame I2S ngắn xuất hiện đúng lúc `tts/start`; Opus
  trả `ESP_AUDIO_ERR_DATA_LACK (-3)`. Đây là frame chuyển trạng thái, không phải
  server/downlink lỗi. Firmware hiện kiểm tra `sample_count` theo
  `app->audio.input_frame_samples` trước khi encode và chỉ bỏ frame ngắn; log
  diagnostics bị giới hạn, không ghi PCM.
- Bản final build/flash bằng ESP-IDF 6.0.2 ở baud `115200`, không
  `erase_flash`: app image `0x148130` (67% app partition còn trống), esptool
  verify hash pass cho bootloader, partition, OTA metadata, model và app. Boot
  serial của image này xác nhận `Found 8MB PSRAM device`, `ST7789 ready 240x280`,
  `startup chime played`, model `wn9_computer_tts`, `wake ... threshold=65`,
  Wi-Fi profile đọc từ NVS, `WebSocket v3 ready` và `server hello accepted`.
- Lượt đầu sau guard dùng report `/tmp/veetee-wake-test-final.json`:
  `wake detected` (2.457 s) → `wake start` (2.468 s) → server chấp nhận audio
  và tạo transcript → `state=speaking` (7.848 s) → `wake capture complete;
  detector re-arm requested` (32.788 s), với `last_ttfa_ms=1416`.
- Lượt xác nhận trên đúng image đã push `c5490cc` dùng report
  `/tmp/veetee-wake-test-c5490cc.json`: `wake detected` (2.262 s) → `wake start`
  (2.269 s) → `state=speaking` (8.105 s) → re-arm (43.559 s). Lượt này cũng
  không có stack overflow, panic, `audio capture read failed`, Opus encode/
  decode error; Voice metrics ghi `protocol_errors=0`, `audio_frames_out=4264`,
  `active_turns=0`, `last_ttfa_ms=1622`.
- Wake→`state=speaking` bao gồm playback utterance, ASR và provider path; đây là
  physical timing evidence, không thay cho M1 warm TTFA gate. Hai lượt cho
  `1416` và `1622 ms`, vì vậy chưa thể coi TTFA dưới 1,5 s là ổn định. Serial
  chứng minh downlink/state flow nhưng không chứng minh người vận hành đã nghe
  rõ loa hoặc nhìn thấy LCD. PTT, AEC/barge-in, false accept/reject và
  long-session soak vẫn chưa được đánh dấu đạt; cần người dùng xác nhận phần
  nghe/nhìn/chạm.

## Regression rerun after final physical build (2026-08-04)

- Voice Server `pytest`: **54 passed**; firmware host CTest: **1/1 passed**;
  physical harness unit tests: **3 passed**; runtime tools: **16 passed**.
- Manager API PostgreSQL: **25 passed**, TypeScript lint/build pass. Một lần
  fail ban đầu ở test conversation persistence là do database host giữ fixture
  cũ cùng timestamp và API page mặc định 20; test đã dùng `limit=100` (giới hạn
  additive được route hỗ trợ), không xoá DB hoặc đổi schema.
- Manager Web typecheck/lint, Vitest **55 passed** và production build pass.

## Physical wake lifecycle fix and 30-turn unattended soak (2026-08-04)

- Owner đã cấp quyền phát audio cho lượt này. Chỉ dùng checkout hiện tại,
  không đổi Wi-Fi/NetworkManager/route máy tính, không `erase-flash` và không
  ghi credential/audio/transcript/report vào Git.
- Soak đầu tiên sau fix recreate chỉ pass hai lượt rồi timeout ở lượt 3 vì
  harness dùng marker `wake capture complete; detector re-arm requested`, được
  ghi trước khi `capture_task` thật sự gọi `vt_wake_arm()`. Marker này không phải
  readiness barrier và không được dùng để bắt đầu audio lượt kế tiếp.
- `capture_task` giờ là owner duy nhất của `vt_wake_arm()` và phát marker mới
  `wake detector armed` sau khi destroy/create model, validate sample rate/chunk/
  word count, apply threshold và set `armed=true` thành công
  (`veetee-firmware/main/main.c:395-404`). Harness/scenario chờ marker mới, nên
  không còn race giữa re-arm và clip wake tiếp theo.
- Lỗi panic cũ trong `iface->clean()` vẫn được tránh bằng lifecycle destroy/create
  của `veetee_wake.c`; mỗi lượt không giữ model instance cũ
  (`veetee-firmware/main/veetee_wake.c:202-249`).
- TTS downlink đã thêm `audio_codec_lock` để serialize Opus encode/decode/reset;
  `tts/start` chỉ reset decoder. Encoder không reset giữa các lượt vì vendor API
  có thể trả `DATA_LACK` ở đầu stream; queue playback được cấu hình qua
  `CONFIG_VEETEE_PLAYBACK_QUEUE_DEPTH=32` và enqueue có backpressure 100 ms
  (`veetee-firmware/main/main.c:232-249,302-339,365-377,475-486`).
- Build ESP-IDF 6.0.2 pass, app image `0x148350` (67% app partition free); flash
  `/dev/ttyACM0` ở `115200` pass hash verification cho bootloader, partition,
  model và app, không xóa NVS.
- Physical report `/tmp/veetee-wake-soak-30-no-enc-reset.json` có **120/120
  event**, đúng **30/30 repetition**, mỗi repetition đủ `wake_detected`,
  `capture_started`, `assistant_speaking`, `wake_rearmed`; một WebSocket session
  được giữ xuyên suốt. Voice `/health/ready` sau soak: `activeConnections=1`,
  `activeTurns=0`, `protocol_errors=0` (counter `activationFailures=1` là lịch
  sử tích lũy, không tăng trong lượt này).
- Hai lượt verbose sau build ghi nhận đôi khi hai frame capture đầu của lượt mới
  trả `Opus capture encode failed`, nhưng server vẫn nhận transcript và phát
  TTS; hiện chưa coi đây là lỗi mất lượt, nhưng cũng chưa tuyên bố audio frame
  path hoàn hảo. Cần instrument/benchmark thêm encoder warm-up và kiểm tra
  packet loss trước khi M1 SLO audio được đóng. Soak 30 vòng chạy non-verbose,
  nên report không phải bằng chứng rằng không có warning serial ngoài các marker.
- Host regression sau thay đổi: Voice Server **54 passed**, firmware CTest
  **1/1 passed**, physical harness **4 passed**. Serial/build evidence này không
  thay thế owner xác nhận nghe loa, nhìn LCD, PTT, false accept/reject, AEC và
  acoustic barge-in; các gate đó vẫn mở.
- Sau commit/push `3f15b26`, firmware được build lại với app version đúng
  `3f15b26` rồi flash lại ở `115200` (không erase NVS). Smoke exact-image
  `/tmp/veetee-wake-exact-3f15b26.json` có **8/8 event trên 2/2 repetition**;
  serial xác nhận `wake detector armed` sau mỗi recreate và Voice health vẫn
  `activeConnections=1`, `activeTurns=0`. Đây là verification của artifact đã
  push, không phải một mốc code mới.

## Physical audio permission smoke — full wake-to-speaker path (2026-08-04)

- Owner đã cho phép phát audio. Harness `tools/physical/wake_audio_test.py`
  phát clip wake `Computer` rồi clip tiếng Việt qua loa máy vào mic ESP32;
  không đổi Wi-Fi/NetworkManager/route, không erase NVS và không sửa source
  firmware trong lượt này.
- Lượt dùng snapshot Manager production trước tiên đi tới `wake detected` →
  `wake start` → server nhận transcript, nhưng Groq trả
  `LLM_RATE_LIMITED`; không thêm fallback hoặc key rotation vào runtime.
- Để kiểm tra phần loa mà không làm thay đổi config sản phẩm, đã chạy một
  snapshot **test-only** tạm thời (cùng ASR/VAD/VieNeu/protocol, một key test
  khả dụng), sau đó dừng và khởi động lại Voice Server từ `config_source=manager`.
  File snapshot/key tạm nằm ngoài repository và đã xoá sau test.
- Report `/tmp/veetee-wake-audio-fixture.json` ghi đủ **4/4 event**:
  `wake_detected` (2.247 s) → `capture_started` (2.248 s) →
  `assistant_speaking` (8.070 s) → `wake_rearmed` (32.632 s). Serial có
  `wake capture paused while server is speaking` và không thấy panic/stack
  overflow/Opus error trong lượt verbose.
- Sau restore, Voice Server trả `revision=80`, checksum production
  `9a35728d…514a7a`, `activeConnections=1`, `activeTurns=0`,
  `activationFailures=0`, `protocol_errors=0`; Git worktree sạch. Serial
  chứng minh downlink/state path đã chạy, nhưng việc người vận hành nghe rõ
  loa hoặc nhìn thấy LCD vẫn cần xác nhận vật lý trực tiếp.

## Physical wake lifecycle with configurable noise suppression (2026-08-04)

- Artifact firmware `d01b4af` được build/flash lại bằng ESP-IDF 6.0.2 ở
  `115200`, không `erase_flash`; NVS/Wi-Fi giữ nguyên. Kconfig runtime bật
  `CONFIG_VEETEE_NOISE_SUPPRESSION_ENABLED=y` và mode `1`.
- Lượt full lifecycle `/tmp/veetee-wake-ns-full.json` đã pass đủ **4/4 event**:
  `wake_detected` **2.151 s** → `capture_started` **2.152 s** →
  `assistant_speaking` **7.797 s** → `wake_rearmed` **35.469 s**. Serial có
  `wake_ready=1`, `capture=1` trong lúc thu; Voice metrics sau lượt ghi
  `last_ttfa_ms=1223`, `protocol_errors=0`, `active_turns=0`.
- Đây là bằng chứng host/serial cho NS + wake/capture + downlink lifecycle.
  Chưa coi là AEC/acoustic barge-in vì pin map hiện tại là simplex I2S; vẫn cần
  test PTT, interrupt, false accept/reject và owner xác nhận trực tiếp loa/LCD.

## Owner-approved audio retry against production snapshot (2026-08-04)

- Sau khi owner xác nhận có thể phát audio, chạy lại harness với `--allow-audio`
  và `--verbose`. Clip wake tiếng Anh được phát từ máy host vào microphone
  ESP32; serial ghi nhận `wake detected` ở **2.166 s**, `wake start` ở
  **2.173 s**, sau đó `server accepted audio and produced transcript`.
- Lượt này không đi tới `state=speaking`: firmware nhận
  `server alert code=LLM_RATE_LIMITED` và harness timeout marker speaking sau
  45 s. Đây là lỗi quota/secret của LLM snapshot đang chạy, không phải lỗi
  phát audio hoặc wake/capture. Không đổi provider, không thêm fallback/key
  rotation và không sửa persistent Manager config trong lượt này.
- Test-only `tools/groq_probe.py` đã gọi model đang publish (`llama-3.3-70b-versatile`)
  với **4 key** trong `secrets/groq.keys`; cả 4 trả HTTP 200. Runtime production
  vẫn dùng một `secretRef` của Manager (không tự động lấy danh sách test key),
  nên cần một lượt test-only snapshot/key riêng nếu muốn lặp lại full speaker
  path mà không thay đổi cấu hình sản phẩm.
- Health sau lượt: `revision=80`, `activeConnections=1`, `activeTurns=0`,
  `protocol_errors=0`. Lượt timeout không tạo report hoàn chỉnh; mọi log/report
  tạm đều ở `/tmp` và không được đưa vào Git.

## Provider resource benchmark with test-only secret (2026-08-04)

- Chạy một Voice Server tạm ở `127.0.0.1:18102` từ snapshot revision `80`,
  giữ nguyên PhoWhisper `int8_float16` CUDA, Silero ONNX CPU và VieNeu v3 Turbo
  ONNX INT8; chỉ bỏ `secretRefs` trong bản snapshot tạm để đọc đúng một key
  test từ `/tmp`. Manager snapshot production, database `veetee_vubq` và
  process `18100` không bị sửa.
- `realtime_lab.py` chạy 5 turn với audio tiếng Việt: turn đầu có `tts.start`,
  484 packet downlink, không protocol error và TTFA **1.313 s**. Bốn turn kế
  tiếp đều nhận `LLM_RATE_LIMITED` từ cùng key, nên report không đạt p95; không
  thêm fallback/key rotation vào runtime. Đây là evidence của failure policy,
  không phải kết luận provider/model promotion.
- `nvidia-smi` sample 0,5 s trong toàn bộ process: **881 MiB peak / 4.096 MiB**
  VRAM (mức nền khoảng 397 MiB trước benchmark); thấp hơn promotion envelope
  3.500 MiB. Không có dấu hiệu VRAM pressure, nhưng cần benchmark lặp lại với
  key/model quota ổn định trước khi promote.
- Process test-only đã dừng, port `18102` không còn listen; Voice production
  `18100` trở về `revision=80`, `activeConnections=1`, `activeTurns=0`,
  `protocol_errors=0`. Key tạm đã được shred; không có credential, raw audio
  hoặc report `/tmp` được commit.

## Test-only Groq key pool — firmware wake/audio verification (2026-08-04)

- Để xác minh nhiều free-tier key trong luồng firmware mà không thay đổi
  production, đã tạo snapshot fixture ngoài Git với `providers.llm.secretRefs=[]`
  và chạy Voice Server tạm tại `18100` với
  `VEETEE_TEST_GROQ_KEYS_FILE=secrets/groq.keys`. Guard ở entrypoint chỉ cho phép
  biến này khi `VEETEE_CONFIG_SOURCE=fixture`; Manager source bị fail-closed.
- Harness `wake_audio_test.py --allow-audio` chạy **4/4 repetition**. Mỗi lượt
  đều có đủ `wake_detected` → `capture_started` → server nhận transcript →
  `assistant_speaking` → `wake_rearmed`; serial có
  `wake capture paused while server is speaking`, không panic/stack overflow.
  Voice metrics cuối lượt: `turn_count=4`, `turn_rejections=0`,
  `protocol_errors=0`, `last_ttfa_ms=1282`, `audio_frames_out=2049`.
- Log test-only chỉ ghi ordinal đã redact: key `1`, `2`, `3`, `4` lần lượt
  thành công ở bốn turn. Unit test ép HTTP `429` trước delta đầu tiên đã xác
  minh pool thử key kế tiếp; unit test stream lỗi sau khi đã phát text xác minh
  không replay partial stream. Lượt physical này không gặp `429`, nên là bằng
  chứng pool được dùng trong firmware flow, không phải bằng chứng quota của
  từng key.
- Sau test, process fixture đã dừng và Voice Server được khởi động lại từ
  `config_source=manager` với Manager revision `82`. Readiness hiện trả
  `activeConnections=0`, `activeTurns=0`, `activationFailures=0`, checksum bắt
  đầu `a398e94b`; không còn `VEETEE_TEST_GROQ_KEYS_FILE` trong production
  environment. Manager API vẫn healthy revision `82`.
- Đây là tiện ích kiểm thử tạm thời, không phải provider fallback/key rotation
  của sản phẩm. Production vẫn giải mã đúng một `secretRef`; nếu key đó bị
  Groq giới hạn, server báo lỗi có mã `LLM_RATE_LIMITED` và không tự lấy danh
  sách key trong file test.

## Configured forbidden-marker gate và long-soak interruption (2026-08-04)

- Physical harness được bổ sung `forbiddenMarkers` theo scenario. Monitor fail
  ngay khi gặp marker cấu hình được (ví dụ panic, stack overflow, queue/capture
  failure hoặc Opus encode error); không nhúng danh sách lỗi vào firmware. Unit
  suite harness hiện **5/5 passed**. Scenario example đã bật gate này.
- Đã chạy fixture-only soak `100` repetition với wake clip `Computer`, utterance
  tiếng Việt và 4 Groq key. Lượt chạy qua **63 repetition đầy đủ** và bắt đầu
  repetition 64; đến stage `capture_started` của lượt 64, harness không nhận
  `state=speaking` trong 45 s nên dừng fail-closed. Không có forbidden marker
  nào xuất hiện trước khi dừng.
- Voice metrics tại thời điểm dừng: `turn_count=70` (6 turn preflight + 64
  turn soak), `turn_rejections=0`, `protocol_errors=0`,
  `audio_frames_in=5674`, `audio_frames_out=21820`. Có một lần reconnect trong
  process (`session_admissions=2`, `session_releases=1`); đây là tương quan
  duy nhất hiện có với lượt mất marker, chưa đủ để kết luận root cause. Cần
  chạy reconnect/session trace riêng trước khi đánh dấu M1 long-session DoD.
- Test pool đã gặp nhiều HTTP `429` trước delta đầu tiên trên key ordinal 1;
  ordinal 2 tiếp tục thành công, các key 3/4 cũng hoàn tất các turn tương ứng.
  Không có partial-stream replay và không có quota failure làm dừng soak.
- Resource sample trong lượt: RSS Voice khoảng `1.73–1.76 GiB`, GPU tổng
  quan sát `397 MiB` với một mẫu `493 MiB`; không có CUDA OOM, panic hoặc queue
  growth được chứng minh. Các số này là observation của fixture process, không
  phải promotion gate cuối.
- Fixture process đã dừng sau failure và production Voice Server được restore
  từ Manager revision `84`, readiness `activationFailures=0`,
  `activeConnections=1`, không có `VEETEE_TEST_GROQ_KEYS_FILE`. DoD 60-minute
  soak tại lượt này **chưa đạt**; không coi lượt partial này là pass. Gate đã
  được chạy lại sau cleanup hardening ở mục kế tiếp.

## Disconnect cleanup hardening sau soak interruption (2026-08-04)

- Host reproduction với `SlowLLM` cho thấy khi aiohttp request task bị cancel
  trong lúc peer đóng WebSocket, provider task có thể release turn lease trước
  khi session `finally` chạy xong; metric disconnect abort không được ghi nhận.
  Regression mới `test_disconnect_aborts_active_turn_and_releases_lease` đã bắt
  đúng trường hợp này.
- `VoiceSession.run()` hiện chạy `_cleanup()` trong child task được
  `asyncio.shield`; nếu request task bị cancel, nó vẫn chờ cleanup hoàn tất.
  Cleanup hủy provider/MCP generation, abort turn, report disconnect và release
  session/view lease trước khi propagate cancellation. Không replay audio hay
  tự retry turn cũ.
- Metric `turn_disconnect_aborts` được thêm để phân biệt turn bị mất do
  transport với provider error. Test mục tiêu và handshake regression đều pass
  **2/2**; full Voice Server suite sau patch **57 passed**.
- Đây là hardening cho reconnect/session ownership, chưa biến một Wi-Fi drop
  thành tự động gửi lại câu hỏi. Physical soak 100 lượt phải chạy lại sau khi
  artifact server mới được verify; lượt 64 cũ vẫn được giữ là failure evidence.

## Fixture physical soak sau cancellation-safe cleanup (2026-08-04)

- Firmware source/wire không đổi sau lần flash trước; `idf.py build` bằng
  ESP-IDF **6.0.2** pass, binary app còn **67%** partition. Không flash lại và
  không erase NVS vì gate này chỉ thay Voice Server.
- Voice Server tạm chạy snapshot fixture revision `1` (PhoWhisper CUDA,
  Silero ONNX, VieNeu v3 Turbo) với `VEETEE_TEST_GROQ_KEYS_FILE` gồm 4 key.
  Hai lượt smoke trước soak pass đủ wake/capture/transcript/speaking/rearm;
  `last_ttfa_ms=1181`, `protocol_errors=0`.
- Harness `wake_audio_test.py --allow-audio` chạy từ `06:35:28Z` đến sau
  **60 phút** (event cuối ở `3.742.076 ms`). Có **94 repetition hoàn chỉnh**;
  repetition 95 đã bắt đầu nhưng bị dừng có chủ ý sau khi đủ 60-minute gate.
  Không có `forbiddenMarkers`, panic, stack overflow, queue/capture hoặc Opus
  error. Repetition 64 — failure point của lượt cũ — lần này đã nhận
  `state=speaking` và `wake_rearmed` bình thường.
- Metrics cuối fixture sau cleanup: `turn_count=97`,
  `turn_admissions=97`, `turn_releases=97`, `active_turns=0`,
  `turn_rejections=0`, `protocol_errors=0`, `audio_frames_in=7514`,
  `audio_frames_out=54069`, `session_admissions=2`, `session_releases=1`,
  `turn_disconnect_aborts=1`. Một reconnect đã xảy ra nhưng không làm mất
  lifecycle kế tiếp; không có stale audio được phát hiện.
- Key pool gặp HTTP `429` thật ở ordinal 1 nhiều lần; ordinal 2 tiếp tục thành
  công, sau đó ordinal 3/4 lần lượt chạy. Không replay partial stream. RSS Voice
  dao động khoảng **1,90–1,93 GiB**, GPU **397 MiB/4096 MiB**, không có CUDA OOM
  hoặc tăng queue đơn điệu.
- Fixture service đã dừng. Voice production được khởi động lại từ Manager
  revision `86`, checksum `1a715711…59d1bc44`, `activeConnections=1`,
  `activeTurns=0`, `activationFailures=0`; environment production không có
  `VEETEE_TEST_GROQ_KEYS_FILE`. Không thay Wi-Fi, NetworkManager, route,
  database/schema hay secret.
- Đây là **pass cho 60-minute fixture physical soak**, chưa phải M1 hoàn tất:
  100 repetition chưa chạy đủ, AEC/acoustic barge-in, false accept/reject
  corpus, cross-peer physical conformance và provider promotion vẫn mở. Vì
  lượt này dùng harness trước khi có partial-report hardening nên SIGINT không
  ghi report cuối; metrics/journal summary tạm đã được giữ ngoài Git để làm
  evidence. Harness hiện đã ghi `status=passed|failed|interrupted` và partial
  events khi có `--report`.

## Physical harness partial-report hardening (2026-08-04)

- `wake_audio_test.py` nhận một result sink bounded theo vòng đời chạy và luôn
  ghi report redacted cho cả đường thành công, `HarnessError` và SIGINT. Report
  mới thêm `status`/`error` additive; không chứa audio, secret hoặc raw serial
  ngoài event marker/timestamp cần thiết.
- Unit/runtime harness suite sau thay đổi: **20 passed**; dry-run vẫn không mở
  monitor/player. Đây là tooling evidence, không thay đổi firmware, wire,
  provider selection hoặc network.

## Deterministic long-text streaming gate (2026-08-04)

- Bổ sung `veetee-server/tests/test_pipeline.py::test_long_text_streams_thirty_minutes_without_collecting_audio`.
  Fixture phát tuần tự **30.000 semantic segment**, tương đương **30 phút audio**
  ở cadence 60 ms; test chỉ đếm frame/control event, không giữ toàn bộ audio trong
  list. Memory excerpt vẫn bị giới hạn 120 ký tự và transcript turn bị giới hạn
  128 segment.
- Codec/TTS trong test là double tối thiểu để đo ownership/segment/packetizer,
  nên đây là host deterministic gate cho pipeline, không phải promotion evidence
  của VieNeu/Opus artifact thật. VieNeu long-text, RSS/VRAM và physical speaker
  acceptance vẫn mở trong M1.
- Targeted pipeline run: **2 passed** (long answer bounded + 30-minute stream
  gate). Không đổi firmware, wire protocol, provider selection, database hoặc
  network.

## Shared protocol golden oracle (2026-08-04)

- C firmware host test và Python Voice Server test cùng đọc
  `tests/fixtures/ws_audio_golden.csv` cho ba framing `ws-v1-compat`, `ws-v2` và
  `ws-v3`; trước đó mỗi bên giữ một bản expected bytes riêng. CMake truyền path
  tuyệt đối vào host test, còn Python đọc CSV trực tiếp.
- Host CTest và Python protocol tests xác nhận encode/decode round-trip và exact
  wire bytes từ cùng một fixture. Đây là gate chống drift giữa hai implementation,
  **không** được gọi là cross-peer pass với backend/firmware reference thật; hai
  `WIRE-CROSS-001/002` vẫn cần peer supplied và physical/e2e evidence.

## Soak failure and async-stream cleanup hardening (2026-08-04)

- Fixture physical run dùng snapshot revision `1`, VieNeu/PhoWhisper thật và
  `VEETEE_TEST_GROQ_KEYS_FILE` 4 key đã hoàn tất **7 repetition** (28/28 event),
  sau đó fail-closed khi chờ `wake detected` của repetition kế tiếp. Report
  ngoài Git ghi `status=failed`; không có forbidden marker, `protocol_errors=0`,
  `turn_releases=7`, và không có server OOM. Đây là failure evidence, không phải
  pass 100 lượt; nguyên nhân wake miss cần corpus/timing investigation riêng.
- Cùng log đó lần đầu lộ `asyncio Task exception was never retrieved` với
  `aclose(): asynchronous generator is already running` khi một turn bị
  interrupt trong lúc Groq stream còn mở. `TurnPipeline` nay join reader task
  rồi đóng explicit async iterator, retry bounded cho race và trả typed
  `LLM_STREAM_CLOSE_FAILED` nếu không đóng được.
- Regression `test_cancelled_turn_closes_async_llm_stream_after_reader_join` và
  toàn bộ Voice Server suite sau patch: **59 passed**. Fixture physical smoke
  mới **10/10** pass (40/40 event), `turn_count=10`, `turn_releases=10`,
  `protocol_errors=0`, không còn `aclose` warning hoặc forbidden marker.
- Sau smoke, production Manager snapshot được restore revision `86`, checksum
  `1a715711…59d1bc44`, `activeConnections=1`, `activeTurns=0`,
  `activationFailures=0`. Không flash firmware, không erase NVS, không đổi
  Wi‑Fi/NetworkManager/route/database/secret.

## Physical harness stage-boundary hardening (2026-08-04)

- `Monitor.wait_for()` nay nhận mốc monotonic `not_before`; mỗi stage chỉ được
  match serial marker xuất hiện từ stage trước trở đi. Marker cũ còn trong queue
  không thể làm repetition mới pass nhầm. Lỗi timeout được gắn rõ
  `repetition N stage <name>` để report failure có thể điều tra được.
- Bổ sung unit test marker stale/current; physical/runtime tooling suite sau thay
  đổi: **21 passed**. Thay đổi chỉ thuộc tooling, không sửa firmware, audio clip,
  server protocol hoặc network.

## 100-repetition retry with stage boundaries (2026-08-04)

- Chạy lại scenario 100 repetition sau cả stream-cleanup và harness boundary
  hardening. **10 repetition hoàn chỉnh** (40/40 event), bao gồm repetition 8
  là điểm fail của lượt trước; lượt 11 dừng fail-closed ở
  `repetition 11 stage wake_detected` sau timeout.
- Tại thời điểm dừng: fixture `turn_count=10`, `turn_admissions=10`,
  `turn_releases=10`, `active_turns=0`, `protocol_errors=0`, không forbidden
  marker, không `aclose` warning/OOM. Key ordinal 1 gặp HTTP 429 nhiều lần và
  test pool tiếp tục dùng key kế tiếp. Đây là bằng chứng wake false-reject hoặc
  timing/player chưa phân biệt được, không được quy kết cho server và không pass
  M1 100-repetition gate.
- Production đã restore Manager revision `86`, `activeConnections=1`,
  `activeTurns=0`, `activationFailures=0`; không flash/erase NVS hoặc đổi mạng.

## Wake false-reject diagnostic controls (2026-08-04)

- Diagnostic run verbose **20 repetition** với cùng clip/firmware đã hoàn tất
  **15 lượt** rồi fail ở `repetition 16 stage wake_detected`. Serial tại vùng
  failure vẫn có mức tín hiệu wake (peak khoảng 12.650), không có forbidden
  marker; Voice metrics kết thúc ở `turn_count=15`, `turn_releases=15`,
  `protocol_errors=0`.
- Control run đổi riêng `interRepetitionDelaySeconds` từ `0,75` lên **5 giây**
  vẫn fail ở repetition 6 sau 5 lượt hoàn chỉnh. Vì vậy chưa đủ bằng chứng để
  quy lỗi cho pacing/echo hay threshold; không tự sửa threshold, model hoặc AFE.
- Kết luận tạm: đây là open physical wake false-reject/timing gate cần corpus
  wake positive/negative và instrument playback/AFE; không phải server/provider
  failure. Production revision `86` đã restore sau mỗi run với ESP32
  `activeConnections=1`, `activeTurns=0`.

## Playback exit instrumentation (2026-08-04)

- Physical harness `tools/physical/wake_audio_test.py` hiện ghi event
  `audio_player_exit` sau mỗi clip hoàn tất. Event chỉ giữ `clipRole`, exit code,
  thời lượng process và tối đa 512 ký tự stderr; không giữ raw audio, microphone
  capture, credential hoặc command line chứa secret.
- Nếu repetition fail ở `wake_detected` nhưng event wake trước đó có
  `exitCode=0`, host player đã hoàn tất và lỗi còn lại thuộc vùng AFE/WakeNet,
  timing hoặc acoustic path; nếu exit code khác 0 thì fail được quy về player
  trước khi kết luận detector. Đây là instrumentation, chưa thay thế positive /
  negative wake corpus hoặc AEC acceptance.
- Unit tooling suite bổ sung kiểm tra exit code/stderr và giới hạn artifact;
  chưa chạy lại physical soak dài chỉ vì thay đổi harness.

## Positive/negative wake corpus — first bounded run (2026-08-04)

- Thêm `tools/physical/wake_corpus_test.py` với corpus JSON cấu hình marker,
  expected outcome, timeout và completion clip; không đưa phrase/threshold vào
  code. Negative case chạy trước positive để tránh cố ý để board ở capture state.
- Physical report `/tmp/veetee-wake-corpus.json` đạt **2/2 case**:
  `negative-vietnamese-utterance` giữ `wake_not_detected` trong cửa sổ 8 giây;
  `positive-computer` đi đủ `wake_detected` → `capture_started` →
  `assistant_speaking` → `wake_rearmed`. Tất cả ba playback process có
  `exitCode=0`, stderr rỗng; không có forbidden marker.
- Serial positive clip ghi peak khoảng `11.429`, còn negative clip có peak tối đa
  khoảng `8.249` nhưng không đánh thức detector. Đây chỉ là một corpus nhỏ, chưa
  đủ để ước lượng false-accept/false-reject theo môi trường; không đổi threshold,
  model hoặc AFE dựa trên lượt này.
- Runtime sau run: Voice/Manager vẫn ready revision `86`, `activeTurns=0`,
  `protocol_errors=0`. Raw report/log và audio vẫn nằm ngoài Git.

## Wake corpus reruns and quota separation (2026-08-04)

- Sau khi sửa timestamp player và đổi marker re-arm sang `wake detector armed`,
  run có report `/tmp/veetee-wake-corpus5.json` đạt **2/2 case**. Negative
  playback `exitCode=0`, duration khoảng `8.001 s`, không có `wake_detected`;
  positive wake/completion đều `exitCode=0`, duration lần lượt khoảng `2.026 s`
  và `3.329 s`, đi đủ tới `wake_rearmed`.
- Một rerun trước đó dừng ở `state=speaking`; metrics ghi
  `provider_error_LLM_RATE_LIMITED=1`. Sau khi thêm `markers.completionFailed`,
  alert được phân loại riêng (`turn_completion_failed`) và board vẫn được đợi
  tới `wake detector armed`; quota failure không còn bị tính thành detector
  false-reject. Production test hiện đã có hai lần `LLM_RATE_LIMITED` trong
  metrics, không có provider fallback/key rotation.
- Có một lượt nhỏ khác timeout ở `wake_detected` sau case negative pass. Đây là
  evidence một wake miss cần điều tra tiếp, không đủ mẫu để tính false-reject và
  không phải lý do để tự đổi threshold/model/AFE. Vì vậy M1 wake corpus gate vẫn
  mở dù run cuối 2/2 pass.
- Sau các lượt: Voice/Manager ready revision `86`, `activeTurns=0`,
  `protocol_errors=0`; không flash, erase NVS hoặc thay đổi mạng.

## Server barge-in control timing metric (2026-08-04)

- Realtime server hiện expose thêm `barge_in_count` và
  `last_barge_in_control_ms`. Metric đo từ lúc đủ `bargeIn.minSpeechFrames` tới
  khi `tts/stop(reason=barge_in)` hoàn tất ở server rồi tạo turn realtime mới;
  nó **không** được gọi là acoustic speaker mute latency.
- Integration test `test_realtime_barge_in_cancels_old_turn_before_new_audio`
  kiểm tra `barge_in_count=1`, control timing dưới 250 ms trong fixture và zero
  stale binary trước barrier. AEC, DAC drain và time-to-silence vật lý vẫn cần
  đo riêng trên ESP32; không thay đổi firmware hoặc wire bắt buộc.

## Runtime reload after barge-in metric (2026-08-04)

- Voice Server `18100` đã graceful reload từ source mới dưới user-systemd
  transient unit `veetee-voice-18100.service` để không bị shell cleanup;
  Manager API/Web/PostgreSQL không restart. Prewarm và `/health/ready` revision
  `86` pass, ESP32 reconnect lại một session.
- Runtime `/metrics` sau reconnect expose `barge_in_count=0`,
  `last_barge_in_control_ms=0` (chưa có physical realtime barge-in trong lượt),
  `active_turns=0`, `protocol_errors=0`. Secret chỉ được tham chiếu bằng file,
  không ghi giá trị vào log/note.

## Test-only Groq multi-key verification (2026-08-04)

- `secrets/groq.keys` hiện có **4 key**; `tools/groq_probe.py` gọi model đang
  publish (`llama-3.3-70b-versatile`) với từng ordinal và nhận `HTTP 200` cả
  **4/4**. Probe chỉ ghi số thứ tự, status và latency đã làm tròn; không ghi
  bearer token hay response text.
- Physical harness chạy lại với snapshot fixture ngoài Git và
  `VEETEE_TEST_GROQ_KEYS_FILE`, firmware đã flash half-duplex wake gate. Lượt
  này dùng A/B `sdkconfig` với noise suppression **off** để chẩn đoán; sau khi
  thu evidence tôi đã khôi phục noise suppression **on**, build/flash lại và
  serial sau reboot vẫn báo `wake_ready=1`, không panic. Lượt
  `wake-test-2.local.json` đạt **2/2 repetition**: mỗi repetition đi đủ
  `wake_detected` → `wake start` → transcript/server speaking → `wake detector
  armed`; audio player đều `exitCode=0`, không forbidden marker, panic hoặc
  protocol error. Voice metrics sau lượt ghi `turn_count=6`, `turn_releases=6`,
  `active_turns=0`, `protocol_errors=0`, `last_ttfa_ms=1101`.
- Log test-only ghi ordinal `1` rồi `2` cho hai turn mới; đây là bằng chứng
  firmware flow thực sự dùng key pool. Lượt này không gặp `429`, nên không được
  diễn giải thành bằng chứng mỗi key có quota độc lập.
- Nhiều key vẫn có thể cùng bị giới hạn bởi model/account/organization hoặc
  cửa sổ quota chung. Pool chỉ là test harness: phân phối round-robin và thử
  key kế tiếp khi `429` xảy ra trước delta đầu tiên; production vẫn một
  `secretRef`, không automatic fallback/key rotation.
- Firmware playback-drain re-arm đã được harden thêm: wake/PTT interrupt reset
  playback queue **trước** khi đánh dấu re-arm pending, tránh mất callback khi
  queue có packet nhưng playback task chưa lấy. Host CTest `protocol_state` pass;
  sau lần flash cuối board giữ `wake_ready=1`, stack headroom khoảng 31 KB và
  không ghi panic/Opus/queue error trong serial window.

## Final noise-on physical revalidation (2026-08-04)

- Sau khi khôi phục `CONFIG_VEETEE_NOISE_SUPPRESSION_ENABLED=y` và flash lại
  firmware có playback-drain re-arm, chạy lại `wake-test-2.local.json` với cùng
  fixture test-only và 4-key pool đạt **2/2 repetition**. Cả hai lượt đều đi đủ
  wake → capture → server speaking → wake detector armed; không forbidden marker,
  panic, Opus/queue error hoặc player failure.
- Voice metrics test-only: `turn_count=2`, `turn_releases=2`,
  `protocol_errors=0`, `active_turns=0`, `last_ttfa_ms=1338`. Log ghi ordinal
  key `1` rồi `2`, nhưng không có `429` nên đây là evidence flow + round-robin,
  không phải quota độc lập.
- Test-only process đã dừng; production Manager snapshot revision `86` đã chạy
  lại, `/health/ready` pass, ESP32 reconnect và gửi presence thành công. Runtime
  production không có `VEETEE_TEST_GROQ_KEYS_FILE`.

## AEC adapter and wake-during-playback verification (2026-08-04)

- Firmware thêm module `veetee_aec.c` theo ADR-020. ESP-SR `afe_aec` được khởi
  tạo với input `MR`, mic 16 kHz, playback reference resample từ 24 kHz, chunk
  runtime **512 samples** và bounded ring **8000 samples (500 ms)**. AEC init
  fail-closed về PTT/half-duplex; `tts/start` và interrupt flush reference ring
  để không kéo audio cũ sang generation mới. Wire Opus vẫn 16 kHz/24 kHz, 60 ms.
- ESP-IDF 6.0.2 build/flash không `erase-flash` pass; serial xác nhận `AEC ready`,
  WakeNet/LCD/Wi-Fi/WebSocket v3 ready, app còn **66%** partition, stack headroom
  khoảng **31 KB**, không panic/queue/Opus error.
- Với production single-key server, lượt 10-repetition đầu dừng ở repetition 4
  vì `provider_error_LLM_RATE_LIMITED=1`; metrics cho thấy `turn_admissions=6`,
  `turn_releases=6`, `active_turns=0`, không có forbidden firmware marker. Đây
  là quota evidence, không phải AEC failure.
- Chạy lại Voice Server fixture revision `82` với
  `VEETEE_TEST_GROQ_KEYS_FILE` gồm 4 key, AEC và wake-during-playback bật trên
  firmware: **10/10 repetition** đi đủ `wake_detected → wake start →
  state=speaking → wake detector armed`; mọi audio player exit code 0, không
  forbidden marker. Đây là lifecycle/resource evidence dưới quota ổn định; chưa
  phải chứng minh echo-only false-accept/reject hoặc acoustic voice-onset
  time-to-silence.
- Sau test fixture, unit đã dừng và production Manager source revision `86` được
  khôi phục trên port `18100`; Voice/Manager readiness pass, board reconnect
  `activeConnections=1`, production environment không có
  `VEETEE_TEST_GROQ_KEYS_FILE`.

## Acoustic barge-in A/B diagnostic (2026-08-04)

- Harness mới `tools/physical/wake_audio_test.py` có phase `bargeIn` cấu hình
  được: phát clip interrupt sau marker cuối (`state=speaking`), rồi chờ các
  marker lifecycle mới (`wake detected`, `state=listening`, `wake interrupt`,
  `wake start`). Harness chỉ ghi event/timing và exit code của player; không
  ghi raw audio, transcript hoặc secret. Host unit suite đạt **12/12** và
  barge-in dry-run pass.
- Physical report `/tmp/veetee-wake-barge-in-20260804.json` với AEC-on đạt wake
  lần đầu → capture → server speaking; wake lần hai trong lúc speaker nói
  timeout ở `interrupt_wake_detected`. Không có panic, Opus error hoặc player
  failure.
- Có một build/flash tạm AEC-off (`/tmp/veetee-build-aec-off`) để kiểm tra
  control, nhưng Kconfig hiện `VEETEE_WAKE_DURING_PLAYBACK` phụ thuộc bắt buộc
  vào `VEETEE_AEC_ENABLED` (`veetee-firmware/main/Kconfig.projbuild:138-145`).
  Vì vậy AEC-off tự chuyển về half-duplex và **không chạy WakeNet trong lúc
  speaker nói**; timeout tương tự không phải A/B acoustic evidence và không
  được dùng để kết luận AEC là nguyên nhân. Harness đã được bổ sung preflight để
  từ chối scenario `bargeIn` nếu flag này không được bật.
- Sau chẩn đoán đã flash/restore bản AEC-on bằng ESP-IDF 6.0.2, không erase NVS.
  Serial window sau reset vẫn có `wake_ready=1`, stack headroom khoảng 31 KB,
  không panic/queue/Opus error; Voice production `18100` revision `86`, API
  `18101` revision `86`, `activeConnections=1`, `activeTurns=0`,
  `protocol_errors=0`. Production không có `VEETEE_TEST_GROQ_KEYS_FILE`.
- Kết luận gate: server-side barge-in control path đã có test/instrumentation;
  **acoustic wake-during-playback/barge-in chưa đạt**. Cần instrument mic vs
  playback reference và đo time-to-silence/voice-onset trên board trước khi
  đóng M1; không đánh dấu M1 đạt chỉ vì wake lifecycle trước playback vẫn pass.

## AEC transform bypass diagnostic (2026-08-04)

- Lượt verbose AEC-on cho thấy capture task vẫn đọc `960` samples mỗi chu kỳ
  trong `state=speaking` (`capture=0`, `wake_ready=1`, không có
  `capture noise processing skipped`, panic hoặc Opus error), nhưng không tạo
  `wake detected` cho clip interrupt.
- Để kiểm tra riêng giả thuyết AEC làm mất near-end speech, thêm cờ cấu hình
  `CONFIG_VEETEE_AEC_PROCESS_WAKE`. Firmware diagnostic giữ
  `AEC_ENABLED=y` + `WAKE_DURING_PLAYBACK=y` nhưng đặt cờ này `n`; build/flash
  tại `/tmp/veetee-build-aec-bypass` và report
  `/tmp/veetee-wake-barge-in-aec-bypass-20260804.json` vẫn timeout ở
  `interrupt_wake_detected`. Serial bypass cho thấy detector chạy và mức mic
  đạt peak khoảng `3547`, không có forbidden marker.
- Kết luận: AEC transform **không phải nguyên nhân duy nhất**; vẫn chưa đủ
  bằng chứng để kết luận do WakeNet threshold, echo masking, playback-reference
  alignment hay thời điểm phát clip. Cờ bypass chỉ là diagnostic, không được
  dùng cho product build.
- Đã flash lại cấu hình AEC-on mặc định (`AEC_PROCESS_WAKE=y`) không erase NVS;
  serial sau restore `wake_ready=1`, stack headroom khoảng `31 KB`. Runtime
  production vẫn Voice/API revision `86`, `activeConnections=1`, `activeTurns=0`.

## Barge-in timing probe (2026-08-04)

- Harness được mở rộng với `bargeIn.startDelaySeconds` (bounded `0..60`, ghi
  vào report) để không gắn timing vào code. Probe ignored scenario với delay
  `4 s` sau `state=speaking` vẫn timeout ở `interrupt_wake_detected`; serial
  tiếp tục báo `wake_ready=1`, capture task đọc frame và mức sau AEC đạt peak
  khoảng `2935` trong cửa sổ clip. Lượt này không có panic/Opus/player error.
- Vì server vẫn ở `state=speaking` suốt cửa sổ probe, kết quả chỉ loại giả
  thuyết “chỉ phát clip đúng tại onset” ở cấu hình này; chưa phải bằng chứng
  echo-only hoặc time-to-silence. Cần đo chính xác playback end và một control
  clip sau khi detector re-arm để tách echo masking khỏi timing.

## Barge-in lifecycle correction — host verified, physical gate vẫn mở (2026-08-04)

- Source review xác định một blocker có độ tin cậy cao độc lập với chất lượng
  AEC: WakeNet tự đặt `armed=false` sau lần detect đầu. Trước correction,
  firmware chỉ enqueue re-arm sau `tts/stop`/playback idle, nên nhánh capture
  trong `speaking` vẫn gọi detector đã disarm và không thể phát wake event lần
  hai. Điều này giải thích chung cho timeout AEC-on và AEC-transform-bypass;
  không kết luận AEC, threshold hoặc speaker leakage đã tốt/xấu.
- Firmware hiện snapshot interaction mode tại `listen/start`. Với auto mode và
  AEC-ready, `tts/start` enqueue re-arm qua capture-owner trước khi detector
  được phép chạy trong `speaking`; manual PTT playback vẫn half-duplex. `tts/stop`
  được giữ ở `speaking` đến khi decoder + playback queue drain, rồi đi
  `manual → idle` hoặc `auto → listening`. Abort/new `tts/start`/disconnect xóa
  pending graceful stop để frame/control cũ không đổi state của turn mới.
- State host test bổ sung manual/auto/legacy stop và 30 lượt manual restart từ
  idle; CMake/CTest pass **1/1**. ESP-IDF 6.0.2 build với
  `WAKE_DURING_PLAYBACK=y` pass và sinh firmware binary; chưa flash board, chưa
  phát audio, chưa đo acoustic barge-in hay time-to-silence từ patch này.
- Bước physical kế tiếp phải flash image đã review, chạy lại scenario wake lần
  hai trong lúc TTS rồi ghi riêng `wake detected → abort → silence`; chỉ khi đó
  mới đánh giá AEC/echo, false accept/reject và M1 gate.

## Firmware stale `tts/stop` barrier and physical re-check (2026-08-04)

- Verbose serial đã tái hiện race: sau `wake interrupt → wake start`, một
  `tts/stop` của turn cũ đến muộn làm `capture=0` và re-arm detector trong khi
  turn mới đang listening. Server vẫn healthy nhưng lượt đó hết hạn
  `NO_SPEECH_TIMEOUT`; report đỏ `/tmp/veetee-wake-test-10-20260804.json` giữ
  bằng chứng redacted.
- Firmware guard tại [`main.c`](../../veetee-firmware/main/main.c:421-476) lưu
  optional `tts.turn_id`, bỏ qua stop khác ID và chỉ tắt capture sau khi state
  speaking được xác nhận. Đây là additive; stop không có ID vẫn đi qua state gate
  để tương thích peer cũ.
- ESP-IDF 6.0.2 build/flash `115200` pass, esptool hash verify pass, không
  `erase-flash`; serial sau flash vẫn có WakeNet ready, capture idle, không panic,
  Opus hoặc queue error. Host CTest **1/1** pass.
- Scenario normal wake 2 lượt với nghỉ 10 giây pass: mỗi lượt
  `wake_detected → wake start → state=speaking → wake detector armed` và player
  exit `0`; report `/tmp/veetee-wake-normal-2-20260804.json`.
- Scenario barge-in physical pass control lifecycle:
  `wake detected → state=listening → wake interrupt → wake start`; report
  `/tmp/veetee-wake-barge-in-guarded-20260804.json`. Đây là lifecycle/control
  evidence, chưa phải acoustic time-to-silence.
- Wake corpus smoke pass gồm 1 negative (`local-utterance-vi.wav`, không detect
  trong 8 giây) và 1 positive (`local-wake-computer.wav`, detect + completion
  turn + re-arm); report `/tmp/veetee-wake-corpus-20260804.json`. Đây chỉ là
  smoke corpus, chưa đủ kích thước để chốt false accept/reject rate.
- Realtime Lab host-only sau firmware guard chạy 4 turn với 1 warm-up; cả 3 warm
  turn có `tts.start`, binary packet, `tts.stop`, không alert/protocol error.
  TTFA warm p50 **1293,6 ms**, p95 **1481,4 ms**, pass gate `<1500 ms`; report
  `/tmp/veetee-realtime-lab-4-20260804.json`. Đây là operational/server evidence,
  không thay thế TTFA đo từ mic/loa vật lý.
- Scenario 10 lượt vẫn có false reject/no-speech ở repetition 2 khi wake trong
  cửa sổ playback; không đánh dấu 100-turn/false-reject gate đạt. Metrics cuối
  lượt barge: `protocol_errors=0`, `activeTurns=0`.

## M0 manual PTT harness boundary after lifecycle correction (2026-08-04)

- Harness M0 `tools/physical/ptt_acceptance.py` được chỉnh theo cùng lifecycle
  manual của `11b307f`: stage cuối bắt buộc là
  `graceful tts drain complete state=idle`, không còn diễn giải
  `state=listening` như graceful manual drain. Nó tách với harness wake/barge-in:
  chỉ xác nhận một operator-run PTT turn và không phát hiện wake trong lúc speaker
  đang nói.
- Harness chỉ quan sát serial bằng `idf.py monitor --no-reset`; GPIO0 luôn do
  operator tác động. Playback clip tiếng Việt cần `--allow-audio` explicit; report
  default đã redact raw serial/audio/transcript/credential và chỉ có marker timing,
  metrics và optional Manager history phrase hash. Host tooling suite hiện
  **20 passed**, dry-run không mở serial, HTTP hay audio player.
- Đây là readiness của test M0, không phải physical result: image lifecycle mới
  chưa có flash/audio/board acceptance trong evidence này. Nó cũng không đo
  wake-during-playback, `wake detected → abort → silence`, acoustic echo hay
  time-to-silence; các M1 gate trong section trước vẫn giữ trạng thái open.

## Profile-aware wire probe and reference-server handshake (2026-08-04)

- `tools/runtime/realtime_lab.py` nhận `--profile 1|2|3`. Một tham số duy nhất
  điều khiển HTTP `Protocol-Version`, `hello.version` và encoder framing: v1
  raw Opus, v2 header 16-byte, v3 header 4-byte. Tool không sniff hoặc tự
  downgrade profile; framing đối chiếu với implementation firmware tham chiếu
  (`references/xiaozhi-esp32/main/protocols/websocket_protocol.cc:24-53`).
- Runtime fixture Veetee đã chạy cùng một WAV qua cả ba profile, mỗi profile một
  turn: **1/2/3 pass**, lần lượt có `tts.start`, **64** binary packet,
  `tts.stop`, không protocol error; TTFA lần lượt khoảng **87,7 ms**, **81,3 ms**
  và **82,9 ms**. Đây là host fixture evidence, không phải physical board.
- Tool regression và runtime-tool suite: **20 passed** (chạy với
  `PYTHONPATH=tools/runtime`); `git diff --check` pass. Harness không mở
  microphone/serial và không sửa firmware/config/network.
- Đã copy repo server tham chiếu sang `/tmp` và chạy bằng venv riêng, không sửa
  `references/`. Với provider selection rỗng để cô lập wire, client gửi explicit
  profile 1/2/3 cùng `hello`, `listen/start`, binary frame và `listen/stop`; cả
  ba handshake đều nhận `hello` có `session_id` và `audio_params`. Server tham
  chiếu trả `version:1` theo welcome config, phù hợp handler gửi nguyên
  `welcome_msg` (`references/xiaozhi-esp32-server/main/xiaozhi-server/core/handle/helloHandle.py:42-63`).
- Đây mới là **handshake/control smoke** với server tham chiếu. Provider runtime
  của bản copy không được khởi tạo (`TTS` không có selection), nên chưa chứng
  minh ASR→LLM→TTS hoặc firmware thật chạy xuyên peer. Không dùng probe này để
  đóng checklist cross-peer/M1; bước còn lại cần full provider fixture hoặc
  board firmware endpoint thật.

## Firmware session-mismatch barrier (2026-08-04)

- Firmware thêm `vt_wire_session_matches()` và kiểm tra mọi server text message
  sau `hello`: message không có `session_id` vẫn được chấp nhận để tương thích
  peer cũ; field có nhưng sai kiểu, rỗng hoặc khác session hiện tại bị bỏ qua.
  Điều này ngăn `tts`/`alert` của connection cũ tác động lên speaker sau
  reconnect, bổ sung cho stale `tts.turn_id` barrier.
- Host CTest `protocol_state`: **1/1 passed**, gồm match, mismatch, missing và
  malformed/empty cases. ESP-IDF 6.0.2 build pass, app image còn **66%** app
  partition; flash ở `115200` hash verify pass, không chạy `erase-flash`.
- Serial post-flash quan sát được `wake_ready=1`, `capture=0`, stack free khoảng
  **31 KB**, không có panic/Opus/queue error; Voice Server/API vẫn ready revision
  `87`, board reconnect thành công. Đây là host/serial evidence; chưa dùng để
  đóng physical PTT, loa/LCD hoặc acoustic barge-in.

## Server stale-control regression (2026-08-04)

- Bổ sung integration test `test_mismatched_session_control_is_ignored_without_releasing_current_turn`.
  Test gửi `listen/start` và `abort` với session cũ, xác nhận server không cấp
  turn/không giải phóng turn hiện tại; control đúng session sau đó vẫn abort
  được và phát `tts.stop`.
- Voice Server suite sau test: **62 passed**. Đây là server control-plane
  evidence khớp firmware session barrier; không thay thế cross-peer binary thật
  hoặc physical speaker silence.

## Physical no-speech recovery re-check (2026-08-04)

- Chạy `wake_audio_test.py` với scenario `wake-test-no-speech.local.json`, có
  `--allow-audio` theo quyền operator; harness dùng `idf.py monitor --no-reset`,
  không flash, không gửi serial input và không đổi Wi-Fi/NVS.
- Kết quả **passed**: host clip exit `0`; serial sequence
  `wake detected → wake start → server alert code=NO_SPEECH_TIMEOUT → wake detector armed`;
  completion trong khoảng 7,5 giây từ marker capture. Forbidden markers không xuất
  hiện. Report redacted ở `/tmp/veetee-wake-no-speech-20260804-continuation.json`;
  không lưu raw serial/audio/transcript/secret.
- Đây là empty-wake recovery evidence, không đóng acoustic false-reject/AEC,
  time-to-silence hay M0 manual PTT. Các gate đó vẫn cần physical corpus riêng.

## Controlled Groq model TTFA A/B (2026-08-04)

- Chạy hai fixture Voice Server test-only riêng trên `18102`, cùng PhoWhisper
  CUDA/Silero/VieNeu, cùng audio/profile v3, `warmupTurns=1`, 5 warm turns,
  `maxTokens=64` và prompt ngắn; production Manager snapshot/DB/Voice `18100`
  không bị thay đổi. Groq test key pool chỉ được cấp qua
  `VEETEE_TEST_GROQ_KEYS_FILE` với `secretRefs=[]` ngoài Git.
- `llama-3.3-70b-versatile`: TTFA warm p50 **1257 ms**, p95 **1325 ms**;
  `tts.start`/`tts.stop` đủ 5/5, không protocol error. GPU peak **881 MiB**.
- `llama-3.1-8b-instant`: TTFA warm p50 **1273 ms**, p95 **1323 ms**;
  `tts.start`/`tts.stop` đủ 5/5, không protocol error. GPU peak **881 MiB**.
- Một benchmark 70B dài hơn với `maxTokens=256` có 2 TTFA outlier
  `2751/3720 ms`, warm p95 **3720 ms**; không được che outlier. Controlled
  short A/B không đủ để kết luận chất lượng/tool-calling, nên **không đổi
  production model**. Promotion vẫn cần corpus chất lượng, long-answer/stream
  stability và exact resource evidence.
- Hai fixture đã dừng; production Voice `18100` revision `87`, active turns `0`,
  API/Web/Voice readiness `200`. Reports/gpu samples ở `/tmp`, không chứa key,
  raw transcript/audio và không commit.

## Physical wake soak và quota isolation (2026-08-04)

- Scenario 100 lượt dùng clip WakeNet `Computer`, utterance tiếng Việt, profile
  3, monitor `idf.py --no-reset`, khoảng nghỉ 10 giây để tách playback tail khỏi
  lượt kế tiếp. Không giả lập GPIO, không flash, không đổi Wi‑Fi/NVS.
- Production single-key run hoàn thành 15 lượt; lượt 16 nhận wake/capture và
  phát clip nhưng không tới `state=speaking`. Voice metrics ghi đúng
  `provider_error_LLM_RATE_LIMITED=1`, không có panic/queue/Opus error. Report
  redact: `/tmp/veetee-wake-100-delay10-rerun-20260804.json`.
- Test-only multi-key run dùng snapshot revision `87` tạm ngoài Git với
  `VEETEE_TEST_GROQ_KEYS_FILE` và `secretRefs=[]`; 14 lượt hoàn chỉnh, dừng ở
  lượt 15 tại `wake_detected` trước khi có LLM request. Report redact:
  `/tmp/veetee-wake-100-testkeys-delay10-20260804.json`.
- Một lần verbose 3 lượt sau đó pass **3/3**, serial mỗi lượt có đủ
  `wake detected → wake start → server accepted audio → state=speaking → wake
  detector armed`; report/log ở `/tmp/veetee-wake-3-verbose.json` và
  `/tmp/veetee-wake-3-verbose.log`.
- Kết luận: multi-key đã tách được quota production khỏi lỗi detector, nhưng
  false-reject WakeNet/AEC/audio path vẫn chưa đạt gate 100 repetition. Không
  hạ threshold hoặc sửa timing theo một lần fail; cần corpus lặp có đo acoustic
  capture/time-to-silence trước khi promote firmware.

## AEC-transform bypass wake soak (2026-08-04)

- Sau khi giữ lại AEC handle và full-duplex wake policy nhưng tắt riêng bước
  AEC transform (`CONFIG_VEETEE_AEC_PROCESS_WAKE=n`), harness
  `tools/physical/wake_audio_test.py` chạy scenario 20 lượt, delay 10 giây,
  profile 3, threshold 65% và fixture LLM/TTS; operator đã cấp quyền phát
  audio cho lượt kiểm tra này.
- Kết quả **20/20** đủ lifecycle
  `wake detected → wake start → state=speaking → wake detector armed`; không
  có forbidden firmware marker, protocol error hay turn rejection. Report đã
  redact và nằm ngoài Git tại
  `/tmp/veetee-wake-aec-bypass-20-delay10-20260804.json`.
- Kết quả làm hẹp nghi vấn về AEC transform nhưng chưa chứng minh nguyên nhân
  duy nhất: cần A/B có instrumentation reference/playback và corpus acoustic
  riêng trước khi thay đổi production policy. Sau test, scenario đã trả về 100
  repetition, `CONFIG_VEETEE_AEC_PROCESS_WAKE=y`, firmware production đã build
  và flash không erase NVS, rồi Voice Server production Manager revision 87
  được khôi phục với một key Groq, history/presence bật và không có test-key
  pool.
- Từ sau thời điểm này, operator yêu cầu **không phát audio để test** cho tới
  khi có quyền mới. Các bước tiếp theo chỉ được dùng host regression, serial
  read-only, metrics và kiểm tra runtime không âm thanh; không chạy
  `pw-play`/wake harness có `--allow-audio`.

## Wake threshold A/B và corpus re-check (2026-08-04)

- Firmware image threshold `60%` đã chạy với fixture TTS VieNeu, cùng clip,
  profile 3 và khoảng nghỉ 10 giây: **20/20** lượt đủ
  `wake detected → wake start → state=speaking → wake detector armed`, không
  forbidden marker; report redact ở `/tmp/veetee-wake-threshold60-delay10-20260804.json`.
- Cùng điều kiện với threshold `65%`: **20/20** lượt pass; report redact ở
  `/tmp/veetee-wake-threshold65-delay10-20260804.json`. Corpus threshold 60
  gồm một negative utterance và một positive wake cũng pass; report ở
  `/tmp/veetee-wake-corpus-threshold60.json`.
- Một run threshold 60 với khoảng nghỉ `0,75 s` fail ở lượt 2; coi là timing/
  playback-tail diagnostic vì interval khác soak chuẩn, không dùng để hạ
  threshold. A/B 20 lượt chưa đủ chứng minh false-reject rate dài hạn, nên
  **không promote 60%**; `sdkconfig.defaults` và image production giữ `65%`.
- Threshold 65 đã build/flash lại bằng ESP-IDF 6.0.2, esptool verify hash pass,
  không erase NVS; production Voice revision 87 đã khôi phục, fixture/test-key
  unit đã dừng, health ready và board `activeConnections=1`.
- Gate còn mở: 100 repetition có quota-free provider, acoustic echo-only và
  voice-onset barge-in/time-to-silence; các report trên chỉ là evidence bổ sung,
  không đóng M1 DoD.

## 100-repetition quota-free re-check (2026-08-04)

- Chạy lại đúng scenario 100 lượt với threshold 65%, fixture LLM/TTS VieNeu,
  profile 3, delay 10 giây và cùng clip; dừng ở **lượt 3** tại
  `wake_detected` timeout. Hai lượt đầu đủ lifecycle, lượt lỗi không có
  forbidden marker/panic/queue/codec; fixture metrics vẫn `protocol_errors=0`,
  `turn_rejections=0`. Report redact ở
  `/tmp/veetee-wake-100-fixture-threshold65-20260804.json`.
- Kết quả này xác nhận false-reject còn tái hiện khi đã loại quota Groq; không
  quy lỗi cho provider và không hạ threshold. Production Voice Manager revision
  87 đã được khôi phục, test fixture dừng, board reconnect
  `activeConnections=1`.
- M1 gate 100 repetition, acoustic echo-only và voice-onset barge-in/time-to-
  silence vẫn mở; bước tiếp theo cần instrument audio/AEC/WakeNet timing và
  corpus có playback-reference thay vì thêm retry provider.

## Auto-turn watchdog replacement race regression (2026-08-04)

- Bổ sung test host-only `test_auto_no_speech_watchdog_cannot_abort_replacement_turn`:
  một `listen/start mode=auto` được thay bằng turn mới trước deadline phải hủy
  watchdog của generation cũ trước khi turn mới nhận ownership.
- Kết quả: nhóm no-speech **3 passed**; toàn bộ Voice Server suite **63 passed**.
  Sau 1,2 giây không có `NO_SPEECH_TIMEOUT`, turn mới vẫn active và abort đúng
  release lease. Test không phát PCM ra thiết bị, không mở serial và không đổi
  runtime/firmware/config production.
- Đây là race/cancellation evidence bổ sung theo ADR-006/ADR-021; không thay thế
  acoustic barge-in, false-reject corpus, TTFA promotion hay physical acceptance.

## Empty-ASR gate (2026-08-04, host-only)

- Sau `ASR.finish()`, transcript rỗng/whitespace được coi là `no_speech`: server
  gửi `stt` với text rỗng và additive `alert.code="NO_SPEECH"`, sau đó kết thúc
  lượt mà không gọi Intent, LLM hoặc TTS. Alert status/message/emotion đọc từ
  `autoTurn.noSpeechAlert` trong snapshot; core không hardcode thông báo nghiệp vụ.
- Regression `test_empty_transcript_emits_no_speech_without_llm_or_tts` kiểm tra
  call-count của LLM/TTS bằng 0 và finish reason `no_speech`. Voice Server suite
  sau thay đổi: **64 passed**.
- Đây là bằng chứng host/protocol, không phát audio, không mở serial và không
  thay đổi firmware, Wi-Fi, database production hoặc Tailscale. Physical
  microphone/VAD false-reject và loa vẫn là gate riêng chỉ chạy khi operator cấp
  quyền phát audio.

## Non-blocking provider generation warm-up (2026-08-05, host-only)

- Runtime config activation không còn giữ view/lease lock trong lúc
  `ProviderRegistry.prepare()` load model hoặc mở provider pool. Generation cũ
  tiếp tục phục vụ session hiện tại; session có thể acquire/release lease trong
  thời gian candidate đang warm, sau đó snapshot mới được swap atomically.
- Activation được serialize bằng lock riêng; `stop()` chờ activation đang chạy để
  không đóng generation cũ rồi vô tình publish generation mới sau shutdown.
- Regression `test_candidate_prepare_does_not_block_existing_view_leases` giữ
  candidate ở trạng thái chuẩn bị, kiểm tra acquire view cũ vẫn hoàn tất trong
  deadline, rồi mới cho activation commit. Voice Server suite sau thay đổi:
  **66 passed**, Ruff pass; thêm regression cancellation xác nhận candidate
  bị hủy trong warm-up được close và snapshot cũ vẫn active. Regression bổ sung
  cho native/untyped exception xác nhận candidate cũng được close và
  last-known-good view vẫn giữ nguyên; suite hiện **67 passed**.
- Đây là host concurrency evidence, không load model production mới, không
  restart runtime, không phát audio và không thay đổi firmware/Wi-Fi/database.

## History identity redaction (2026-08-05, host-only)

- History reporter trước đây dùng trực tiếp wire `Device-Id` cho `deviceKey`;
  với board thật giá trị này có thể là MAC. Server giờ hash SHA-256 trước khi
  enqueue event, dùng cùng semantics với presence (`identityHash`); wire shape
  và idempotency của Manager API không đổi.
- Regression history xác nhận event chỉ chứa hash ổn định và không chứa raw
  `Device-Id`. Đây là redaction control-plane, không lưu thêm identity vào log,
  transcript hoặc audio.

## Measured resource promotion gate (2026-08-05, host-only)

- Runtime snapshot có thể mang benchmark record `resourceBudget` cho profile GTX
  1650Ti 4 GiB. Server validate record và tính headroom trước khi instantiate
  `ProviderRegistry`; không có OOM rồi retry và không gọi provider thứ hai.
- `BLUE_GREEN` chỉ được phép khi candidate peak delta nằm trong headroom. Khi
  cần dual-residency lớn hơn nhưng generation mới đứng riêng vẫn vừa budget,
  `QUIESCE_SWAP` chỉ chạy khi session lease cũ đã drained; lỗi promotion đóng
  candidate và reload đúng snapshot cũ.
- Resource/runtime regression **24 passed**. Snapshot fixture cũ không có field
  này vẫn tương thích, nên đây chưa phải measured artifact promotion cho
  PhoWhisper/VieNeu thật. M1 resource gate physical/VRAM và soak vẫn mở.
- Slice host-only: không phát audio, không mở serial, không flash firmware và
  không mutate production database/network.

## Idle resource observation (2026-08-05, non-audio)

- Snapshot revision `87` với PhoWhisper CUDA `int8_float16`, Silero CPU và VieNeu
  ONNX INT8 được quan sát 10 mẫu idle liên tiếp: Voice RSS khoảng **1086 MiB**,
  CUDA process memory **344 MiB** trên GTX 1650Ti 4 GiB.
- Đây chưa phải promotion benchmark: candidate peak/dual residency và repeated
  activation chưa chạy. Không publish `resourceBudget` chỉ từ số idle này; M1
  provider/VRAM gate vẫn mở.

## Voice provider snapshot shape hardening (2026-08-05, host-only)

- `ProviderRegistry` fail-closed với providerId thiếu/rỗng và secretRefs sai
  kiểu; trước đây optional selection có thể rơi thành `KeyError`, còn list sai
  có thể bị coi như rỗng. Không fallback/retry provider và chưa allocation model.
- Regression Voice Server **82 passed**, `compileall` và Ruff `src/tests` pass.
- Đây là host/config evidence; không phát audio, không mở microphone/speaker,
  không restart runtime, không flash/reset board. M1 acoustic/physical/provider
  promotion gates vẫn mở.

## MCP argument schema gate (2026-08-05, host-only)

- `DeviceMcpBridge.call()` lấy `inputSchema` từ descriptor cấu hình và validate
  arguments bằng validator generic, không branch theo tên tool/provider.
- Subset wire hiện hành gồm `object`, `array`, `boolean`, `integer`, `number`,
  `string`, `required`, `properties`, `items`, `enum`, range và length/item
  bounds. Schema/argument sai hoặc unsupported fail-closed với
  `TOOL_ARGUMENTS_INVALID` trước khi cấp request ID hoặc gửi envelope xuống
  firmware; duplicate/cancellation/generation semantics hiện tại không đổi.
- Regression `tests/test_mcp.py`: **9 passed**; full Voice Server sau slice:
  **137 passed**, Ruff và compileall pass.
- Không mở MCP/network carrier, không phát/thu audio, không flash/reset ESP32,
  không đổi Wi‑Fi/Tailscale và không mutate production database. Firmware vẫn
  là authority cuối cùng trước hardware mutation.

## Groq text capability probe — test-only, không audio (2026-08-05)

Probe dùng `tools/groq_probe.py` với file key local test-only; output chỉ ghi
ordinal, HTTP status, latency và cờ có response, không ghi key/token. Có **4 key**
được nạp và cả hai model đều trả **4/4 HTTP 200** cho một prompt chào tiếng Việt:

- `llama-3.3-70b-versatile`: latency request **285–507 ms**, response đầy đủ.
- `llama-3.1-8b-instant`: latency request **306–416 ms**, response đầy đủ.

Đây là synchronous capability smoke, không phải streaming first-token/TTFA,
không phải WER/tool-following/VRAM promotion. Production vẫn giữ một
`secretRef`/model selection qua config và không xoay key fallback.

Probe đã bổ sung cờ `--stream`, parse SSE fail-closed và chỉ đo first meaningful
text (không tính tool-call delta hoặc `[DONE]`). Kết quả cùng 4 key:

- `llama-3.3-70b-versatile`: **4/4 HTTP 200**, first text **269–339 ms**, 5 text
  chunks mỗi request.
- `llama-3.1-8b-instant`: **4/4 HTTP 200**, first text **260–359 ms**, 5 text
  chunks mỗi request.

Đây vẫn là probe prompt ngắn qua Internet, không phải Lab E2E TTFA và không
đủ corpus để chọn model production; test harness không thay đổi provider
selection/fallback policy.

Probe `--tool-call` cũng đã kiểm tra forced function-call delta:

- `llama-3.3-70b-versatile`: **4/4 HTTP 200**, tool name `get_weather`,
  arguments JSON hợp lệ, first tool delta **306–512 ms**.
- `llama-3.1-8b-instant`: **4/4 HTTP 200** nhưng không phát tool-call delta
  trong prompt này; không coi là tool-following pass.

Đây là evidence test-only cho model capability; chưa thay thế fragmented-delta
corpus, tool policy/authorization hoặc end-to-end MCP acceptance.

## Groq fragmented tool-call hardening (2026-08-05, host-only)

- Provider parser giờ bỏ qua event SSE malformed/shape sai thay vì `IndexError`
  hoặc `AttributeError`.
- Pipeline giữ argument fragments khi các delta sau không lặp lại
  `function.name` (shape thường gặp của OpenAI-compatible streaming), đánh dấu
  `llm_first_at` ngay ở tool fragment và fail-closed với `TOOL_NAME_MISSING` nếu
  arguments đến mà không có tên.
- Một round phát nhiều tool name khác nhau bị từ chối rõ ràng với
  `MULTIPLE_TOOL_CALLS_UNSUPPORTED`; không trộn arguments của hai mutation.
- Regression provider/pipeline targeted: **28 passed**; full Voice Server:
  **139 passed**, Ruff và compileall pass.

Đây là host/provider contract evidence; authorization, fragmented multi-call
policy và firmware hardware execution vẫn phải pass gate riêng.

## Local provider dependency repair và Voice recovery (2026-08-05, host-only)

- Voice transient service fail-fast sau lần restart trước vì môi trường Python
  thiếu `onnxruntime`, `faster-whisper` và `vieneu`. Khi cài đủ extras, lock cũ
  còn kéo `numba 0.53.1`/`llvmlite 0.36.0` (chỉ hỗ trợ Python <3.10), nên
  `veetee-server/uv.lock` được regenerate có kiểm soát sang `numba 0.66.0`,
  `llvmlite 0.48.0`, `numpy 2.4.6`; không sửa provider logic và không đổi
  selection/fallback policy.
- `uv sync --locked --extra test --extra local-vad --extra local-tts
  --extra local-asr-cuda` hoàn tất. Import smoke pass cho `onnxruntime 1.28.0`,
  `faster-whisper 1.2.1`, `vieneu`, `opuslib`, `numba 0.66.0` và
  `llvmlite 0.48.0`. `_prepare_registry()` với Manager snapshot revision `87`
  (Silero/PhoWhisper/Groq/VieNeu) pass, không ghi credential.
- Voice Server đã khởi động lại qua `veetee-voice-18100.service`; readiness
  `18100`, Manager API `18101` và Web `18181` đều HTTP `200`. Voice revision
  `87`, `activationFailures=0`, `activeTurns=0`, metrics idle không có protocol
  error; VieNeu prewarm thành công, RSS peak service khoảng 1,0 GiB trong
  startup. `activeConnections` có thể thay đổi khi board tự reconnect và không
  được dùng làm physical audio acceptance.
- Regression sau repair: Voice Server **139 passed**, Ruff và compileall pass.
  Toàn bộ bước này là host-only; theo audio lock, không phát/thu audio, không
  mở serial, không flash/reset ESP32, không đổi Wi-Fi/Tailscale và không mutate
  database production.

## Startup activation error boundary (2026-08-05, host-only)

- `RuntimeConfigManager.start()` giờ kiểm tra kết quả activation ban đầu và
  fail-fast với lỗi có type (`runtime configuration activation failed:
  ProviderError/ConfigurationError/ResourceBudgetError`) thay vì khởi động
  poller rồi để `main` báo lỗi mơ hồ `runtime configuration is not ready`.
- Candidate registry vẫn được close transactional khi startup fail; không có
  provider fallback hoặc retry ẩn. Regression runtime **14 passed**, full Voice
  Server **140 passed**, Ruff và compileall pass.
- Đây là startup/error-observability hardening, không thay đổi wire/audio path;
  không phát/thu audio, không mở serial, không flash/reset ESP32 và không mutate
  production database/network.

## Hello feature-shape hardening (2026-08-05, host-only)

- Voice Server giờ fail-closed khi client hello có `features` hiện diện nhưng
  không phải JSON object. Trước đây giá trị như array/string có thể lọt qua
  validation rồi gây lỗi khi đọc `.get("mcp")`; payload hợp lệ bị ảnh hưởng
  không đổi, field `features` vẫn optional để peer cũ tiếp tục chạy.
- Regression WebSocket malformed hello kiểm tra close code `1002`, tăng đúng
  `protocol_errors` và cleanup active connection về `0`, không để exception
  unowned trong event loop.
- Voice Server full suite: **146 passed**, Ruff và compileall pass. Không gọi
  provider/Groq, không phát/thu audio, không mở microphone/speaker/serial,
  không flash/reset ESP32, không đổi Wi-Fi/Tailscale hay production database.

## Audio permission reopened — bounded physical wake and interrupt checks (2026-08-05)

- Chủ dự án đã cấp lại quyền phát audio trực tiếp. Chạy có kiểm soát một lượt
  normal (`wake-test.example.json`) và một lượt wake-word interrupt
  (`wake-barge-in.example.json`) bằng `idf.py monitor --no-reset`; không flash,
  reset, erase, đổi Wi-Fi/NVS/Tailscale hay ghi dữ liệu vào production DB.
- Normal pass: `wake detected → wake start → state=speaking → wake detector armed`;
  cả wake clip và utterance clip exit `0`. Voice Server sau khi drain có
  `turn_admissions=1`, `turn_releases=1`, `active_turns=0`, `protocol_errors=0`.
- Interrupt pass ở firmware: `wake detected → state=listening → wake interrupt →
  wake start`; interrupt player exit `0`, không có forbidden marker. Sau khi
  scenario kết thúc `active_turns=0`, `turn_releases=3`, `protocol_errors=0`.
  Vì scenario không phát utterance thứ hai sau interrupt, `NO_SPEECH_TIMEOUT`
  của auto-turn là hành vi policy đúng, không phải provider failure.
- Đây là wake-word control/lifecycle evidence. `barge_in_count=0` vẫn đúng vì
  counter đó chỉ đếm acoustic voice-onset barge-in phía server; lượt này chưa
  đo AEC, echo-only, speaker time-to-silence hay voice-onset latency. M1
  acoustic barge-in và false-accept/reject corpus vẫn mở.
- Report redact nằm ngoài Git tại `/tmp/veetee-wake-permission-recheck-20260805.json`
  và `/tmp/veetee-wake-barge-in-permission-20260805.json`; không lưu raw audio,
  microphone capture, transcript, serial dump hoặc secret.

## ws-v1 legacy sample-rate compatibility — host/build-only (2026-08-05)

- Rà soát source peer cũ cho thấy server ghi đè `welcome_msg.audio_params` bằng
  client hello; client 16 kHz có thể nhận server hello quảng bá 16 kHz dù speaker
  Veetee mặc định 24 kHz. Firmware trước đây reject mismatch ở mọi profile.
- Thêm helper protocol bounded chỉ chấp nhận các Opus rate chuẩn
  `8/12/16/24/48 kHz`; `ws-v1-compat` cho phép rate hợp lệ khác local decoder,
  còn `ws-v2`/`ws-v3` vẫn strict. Không sniff, downgrade hay đổi default v3.
- Host CTest sau thay đổi: **8/8 passed** với warning flags nghiêm ngặt; ESP-IDF
  6.0.2 build-only pass, image `0x159600`, app partition còn **66%**. Không flash,
  reset, erase hoặc đọc serial/audio.
- Fixture `tests/fixtures/ws_cross_conformance.json` và test
  `veetee-server/tests/test_ws_cross_conformance.py` chạy source-derived hello,
  v1/v2/v3 framing và control không có `session_id`; Voice Server full suite
  **157 passed**, Ruff/compileall pass.
- Đây là host/source oracle, chưa phải cross-process socket với peer ngoài. Các
  checkbox thực thi thật ở §11 của protocol spec và physical v1 peer vẫn mở.

## WakeNet normal 10-repetition soak — physical (2026-08-05)

- Sau khi chủ dự án cấp lại quyền audio, chạy `wake-test-10.local.json` với
  `idf.py monitor --no-reset`, không flash/reset/erase và không đổi runtime
  config. Cả 10/10 repetition đều nhận đủ lifecycle
  `wake detected → wake start → state=speaking → wake detector armed`.
- Wake/utterance player có **0** lần exit lỗi; serial window không có forbidden
  marker. Voice metrics sau khi TTS cuối drain: `turn_admissions=13`,
  `turn_releases=13`, `active_turns=0`, `turn_rejections=0`,
  `protocol_errors=0`, `turn_count=12` (bao gồm các lượt trước trong process).
- Đây là bounded positive wake/lifecycle evidence, không phải false-accept corpus
  hoặc acoustic AEC/voice-onset barge-in. Physical 100-repetition gate, speaker
  time-to-silence và cross-peer v1 socket vẫn mở.
- Report redact ở `/tmp/veetee-wake-10-permission-20260805.json`; không lưu raw
  audio, microphone capture, transcript, serial dump hoặc secret.

## WebSocket hello timeout hardening — host-only (2026-08-05)

- Voice Server giới hạn thời gian chờ client hello bằng
  `VEETEE_HELLO_TIMEOUT_MS` (mặc định 10 giây, range 1–60 giây). Socket idle sau
  HTTP upgrade đóng `1002` và không được admission vào session/provider lease.
- Regression thêm idle peer, metric `hello_timeouts` và validation lower/upper/
  non-integer; hello hợp lệ và các profile v1/v2/v3 không đổi.
- Không dùng audio/Groq, không flash/reset/erase board, không đổi Wi-Fi/NVS/
  Tailscale. Rollback là revert commit slice này nếu cần.

## Audio test after hello-timeout reload — physical (2026-08-05)

- Sau reload service ở commit `8f769b4`, `wake-test-normal.local.json` đạt 2/2
  lượt normal wake với lifecycle đầy đủ; player exit `0`, không forbidden marker.
- Metrics sau drain: `active_turns=0`, `turn_admissions=2`, `turn_releases=2`,
  `protocol_errors=0`, `history_sent=2`. TTFA từng lượt là **1905 ms** và
  **1256 ms**; lượt đầu chưa đạt mục tiêu `<1500 ms`, chưa coi là p95 benchmark.
- Wake-word interrupt đạt `wake detected → state=listening → wake interrupt →
  wake start`; `barge_in_count=0` vì đây chưa phải acoustic voice-onset/AEC test.
  Không có utterance sau interrupt nên `auto_no_speech_timeouts=1` là expected;
  cuối scenario `active_turns=0`, `turn_releases=4`, `protocol_errors=0`.
- Reports redact ngoài Git: `/tmp/veetee-wake-normal-after-timeout-20260805.json`
  và `/tmp/veetee-wake-barge-after-timeout-20260805.json`. Không flash/reset/
  erase, đổi Wi-Fi/NVS/Tailscale hoặc lưu raw audio/transcript/credential.

## Deployed handshake/no-speech verification — host + physical (2026-08-05)

- Live Voice Server probe với peer idle không gửi hello nhận close `1002`; metric
  `hello_timeouts` tăng đúng 1, `protocol_errors` không đổi và không rò session.
- `wake-test-no-speech.local.json` đạt `wake detected → wake start →
  NO_SPEECH_TIMEOUT → wake detector armed`; clip im lặng exit `0`, cuối lượt
  `active_turns=0`, `turn_admissions=5`, `turn_releases=5`,
  `auto_no_speech_timeouts=2`, `protocol_errors=0`.
- Report redact ngoài Git: `/tmp/veetee-wake-no-speech-after-timeout-20260805.json`.
  Không flash/reset/erase, đổi Wi-Fi/NVS/Tailscale hoặc lưu raw audio/transcript.

## Progress acknowledgement TTS ownership — host-only (2026-08-05)

- `TurnPipeline` giờ coi progress acknowledgement và answer là cùng một TTS
  ownership scope: chỉ phát một `tts/start`, giữ cờ `_tts_started` qua progress
  → answer và luôn phát đúng một `tts/stop` nếu LLM kết thúc mà không sinh câu
  hoặc provider lỗi sau acknowledgement. Không thêm câu chữ/provider branch vào
  core; text vẫn đến từ snapshot `progress.acknowledgements`.
- Regression kiểm tra single-start/single-stop khi answer stream chậm và khi LLM
  kết thúc empty sau progress; full Voice Server **163 passed**, Ruff/compileall
  pass.
- Chưa reload/flash/audio trong slice này; runtime deployment chỉ cần graceful
  reload service sau commit. Không đổi wire schema, Wi-Fi/NVS/Tailscale hay DB.

## Progress acknowledgement deployment reload — host-only (2026-08-05)

- `veetee-voice-18100.service` đã graceful-restart để nạp commit `4c27149`;
  readiness HTTP `200`, Manager API/Web không restart, Voice revision `87`,
  `activationFailures=0`, `activeTurns=0`. ESP32 tự reconnect lại, không reset.
- Không phát audio lại cho slice này vì runtime snapshot hiện không có
  `progress.acknowledgements`; physical wake evidence trước đó không thay đổi.

## Physical wake soak re-check and quota isolation (2026-08-05)

- Soak đầu dùng marker `wake detector armed` và dừng ở repetition **37/100**.
  Serial có hai lần re-arm: lúc TTS bắt đầu và sau `graceful tts drain complete`;
  harness có thể phát wake phrase khi loa còn đang nói. Không coi lần này là
  false-reject WakeNet thuần túy. Report redact:
  `/tmp/veetee-wake-100-after-progress-controls-20260805.json`.
- Scenario local bị ignore đã được làm drain-aware: chờ marker
  `graceful tts drain complete state=listening`, rồi mới chờ
  `wake detector armed model=wn9_computer_tts`. Smoke **2/2** pass với marker
  mới: `/tmp/veetee-wake-normal-drain-aware-20260805.json`.
- Production Manager-source (single Groq secret) chạy lại soak drain-aware và
  đạt **20 repetition hoàn chỉnh**; repetition 21 nhận
  `provider_error_LLM_RATE_LIMITED=1` trước `state=speaking`. Metrics lúc dừng:
  `active_turns=0`, `turn_rejections=0`, `protocol_errors=0`; đây là quota
  evidence, không phải firmware crash. Report:
  `/tmp/veetee-wake-100-drain-aware-20260805.json`.
- Để kiểm tra đúng luồng firmware bằng nhiều key như đã yêu cầu, Voice Server
  được chuyển tạm sang snapshot fixture ngoài Git với `secretRefs=[]` và
  `VEETEE_TEST_GROQ_KEYS_FILE` (4 key), sau đó đã restore Manager-source. Fixture
  đạt **3 repetition**; log service ghi ordinal 1→2→3, lần kế tiếp key 1 bị
  HTTP 429 và key 2 thành công. Repetition 4 vẫn timeout ở `wake detected`,
  không có turn mới và không có forbidden serial marker. Report:
  `/tmp/veetee-wake-100-fixture-drain-aware-20260805.json`.
- Verbose fixture smoke sau đó đạt **2/2**; serial giữ `wake_ready=1`, không
  panic/queue/Opus marker, audio player đều exit `0`. Đây là bằng chứng wake
  word có thể nhận và pool test hoạt động, nhưng tỷ lệ false-reject trong soak
  chưa đạt gate 100/100. Không hạ threshold hoặc đổi AEC âm thầm; cần corpus
  false-accept/reject và A/B acoustic có instrumentation trước khi promote.
- Production sau cleanup: Voice `18100` health `ready`, revision `87`,
  `activeConnections=1`, `activeTurns=0`, `activationFailures=0`; Manager API
  vẫn `18101` ready. Không flash/reset/erase, không đổi Wi-Fi/NVS/Tailscale,
  không lưu raw audio, transcript, credential hay snapshot fixture vào Git.

## Audio test permission re-opened — normal turn and barge-in timing probe (2026-08-05)

- Sau khi chủ dự án cấp lại quyền audio, chạy lại `wake-test.example.json` bằng
  production Voice `18100` revision `87`. Normal turn đạt đủ
  `wake detected → wake start → server accepted audio → state=speaking → wake
  detector armed`; wake/utterance player đều exit `0`. Health sau khi drain vẫn
  `ready`, `activeTurns=0`, `protocol_errors=0`, `activationFailures=0`.
  Report redacted: `/tmp/veetee-wake-audio-20260805-1.json`.
- Một lượt barge-in phát clip thứ hai ngay sau marker `state=speaking` không nhận
  `wake detected` trong timeout. Metrics khi lỗi giữ đúng `protocol_errors=0`,
  không có forbidden firmware marker; turn vẫn drain về `activeTurns=0`. Đây là
  bằng chứng race/timing ở cửa sổ re-arm, không phải crash hay provider failure.
  Report: `/tmp/veetee-wake-barge-audio-20260805-1.json`.
- Probe cùng scenario với `bargeIn.startDelaySeconds=2` (giá trị chỉ ở test
  fixture, không đổi firmware/config production) đạt đầy đủ
  `wake detected → state=listening → wake interrupt → wake start`; interrupt
  player exit `0`, không panic/queue/Opus marker. Report redacted:
  `/tmp/veetee-wake-barge-delay-20260805.json.report`.
- Kết luận promotion: firmware đã chứng minh lifecycle wake interrupt khi
  detector đã re-arm, nhưng thời gian từ `tts/start` tới detector sẵn sàng chưa
  được đưa vào contract. Acoustic barge-in tức thời, AEC/echo-only,
  voice-onset latency và time-to-silence vẫn mở; không hạ threshold hoặc ghi
  nhận pass chỉ vì probe delay 2 giây. Không flash/reset/erase, không đổi
  Wi-Fi/NVS/Tailscale, không lưu raw audio, transcript hay credential.

## Wake detector pre-arm trước `tts/start` (2026-08-05)

- Source slice thêm `vt_wake_is_armed()` để capture owner bỏ qua lệnh arm trùng
  và giữ marker tương thích tại
  [`veetee_wake.h:52-59`](../../veetee-firmware/main/veetee_wake.h:52-59),
  [`veetee_wake.c:291-300`](../../veetee-firmware/main/veetee_wake.c:291-300).
- Sau wake event ở trạng thái không interruptible, `ptt_task` gửi lệnh arm ngay
  qua queue; capture task là owner duy nhất gọi `vt_wake_arm()` tại
  [`main.c:613-630`](../../veetee-firmware/main/main.c:613-630) và không feed
  detector khi `capture_active=true`. Wake event xảy ra trong `speaking` vẫn
  dùng playback-drain path cũ tại [`main.c:747-769`](../../veetee-firmware/main/main.c:747-769),
  tránh re-arm lặp và tránh feed phần đuôi của cùng một phrase.
- ESP-IDF 6.0.2 build image `0x1596b0`, app partition còn khoảng 66%; firmware
  host CTest **8/8 passed**. Flash `/dev/ttyACM0` hai lần để nạp image cuối và
  xác nhận hash; chỉ ghi bootloader/partition/OTA/model/app, không `erase_flash`
  và không thay NVS/Wi-Fi. Serial sau boot giữ `wake_ready=1`, stack headroom
  khoảng 31 KB; production Voice revision `87` và Manager API/Web đều ready.
- Physical no-speech scenario sau image pre-arm đạt
  `wake detected → wake start → NO_SPEECH_TIMEOUT → wake detector armed`, không
  forbidden marker. Fixture Voice tạm với 4-key pool (revision `82`, snapshot
  ngoài Git) đạt immediate lifecycle
  `wake detected → state=listening → wake interrupt → wake start`; report
  redacted `/tmp/veetee-wake-barge-prearm-fixture-20260805.json`.
- Production normal scenario sau đó gặp `LLM_RATE_LIMITED` trước
  `state=speaking`; đây là quota evidence của single-key production, không phải
  firmware regression. Production process đã được restore, fixture đã dừng,
  board reconnect lại và `activeConnections=1`.
- Pre-arm giảm race model-recreate nhưng **không đóng acoustic gate**: fixture
  không chứng minh speaker đang phát đủ lớn trong đúng cửa sổ interrupt; AEC
  echo-only, immediate voice-onset, time-to-silence, false accept/reject và M1
  100-repetition vẫn mở. Không hạ threshold hoặc bật fallback provider.

## Audio permission — latest pre-arm firmware with real provider (2026-08-05)

- Sau khi chủ dự án mở lại quyền phát audio, Voice Server được chuyển **tạm
  thời** sang snapshot test ngoài Git với `secretRefs=[]` và
  `VEETEE_TEST_GROQ_KEYS_FILE` (4 key), chỉ để xác minh firmware/audio flow;
  Manager snapshot và database production không bị sửa. Sau mỗi lượt fixture đã
  dừng và production Manager-source được khôi phục.
- Scenario drain-aware `wake-test-normal.local.json` đạt **2/2** lượt normal
  với VieNeu-TTS thật: mỗi lượt đi đủ `wake detected → wake start →
  state=speaking → graceful tts drain complete state=listening → wake detector
  armed model=wn9_computer_tts`. Wake/utterance player đều `exitCode=0`, không
  forbidden panic/stack/Opus marker. Report redact:
  `/tmp/veetee-audio-normal-drain-multikey-20260805.json`.
- Lượt đầu dùng `wake-test-2.local.json` (chỉ chờ `wake detector armed`, không
  chờ playback drain) đạt lượt 1 nhưng timeout ở `wake_detected` của lượt 2 vì
  phát wake chồng lúc VieNeu còn đang drain; report
  `/tmp/veetee-audio-normal-multikey-20260805.json`. Đây là lỗi timing của
  scenario, không phải firmware/provider crash; đã restart fixture và chạy lại
  bằng scenario drain-aware ở trên.
- Metrics fixture sau 2 lượt: `turn_admissions=2`, `turn_releases=2`,
  `active_turns=0`, `protocol_errors=0`, `last_asr_finalize_ms=502`,
  `last_llm_first_token_ms=924`, `last_tts_start_ms=974`,
  `last_ttfa_ms=1461`. Đây là TTFA của lượt cuối, không phải p95 promotion;
  log test-only ghi key ordinal 1 và 2 thành công.
- Scenario `wake-barge-in.example.json` với interrupt clip phát **ngay sau**
  `state=speaking` cũng đạt lifecycle `wake detected → state=listening →
  wake interrupt → wake start`; interrupt player `exitCode=0`, không forbidden
  marker. Report redact: `/tmp/veetee-audio-barge-immediate-multikey-20260805.json`.
  `barge_in_count=0` là đúng vì đây là wake-word interrupt ở firmware, không
  phải server realtime voice-onset; không phát utterance thứ hai nên
  `auto_no_speech_timeouts=1` là policy timeout dự kiến.
- Sau cleanup production Voice revision `87` trả `ready`, config checksum
  Manager `2c02e6…`, board reconnect `activeConnections=1`; Manager API/Web
  vẫn ready. Không flash/reset/erase trong slice này, không đổi Wi-Fi/NVS/
  NetworkManager/route/firewall/Tailscale, không lưu raw audio, transcript,
  credential hoặc snapshot test vào Git.
- Kết luận: pre-arm hiện đã pass positive normal 2-turn và immediate
  wake-word interrupt với provider thật dưới pool test. Acoustic echo-only,
  voice-onset barge-in, speaker time-to-silence, false accept/reject corpus,
  100-repetition và M1 TTFA p95 vẫn **mở**; không đánh đồng lifecycle này với
  acoustic acceptance.

## Config-driven acoustic duplex barge-in — physical probe (2026-08-05)

- Đã thêm policy additive `bargeIn.deviceDuplex`/`minSpeechFrames`, metadata
  `tts/start.barge_in` và barrier `tts/stop(reason:"barge_in")`; snapshot vắng
  field vẫn giữ half-duplex. Decision nằm trong
  [`ADR-026`](../ADR/ADR-026-config-driven-acoustic-duplex-barge-in.md).
- Host regression sau slice: Voice Server **169 passed**, Ruff/compileall pass,
  firmware host CTest **8/8**. ESP-IDF 6.0.2 build image `0x159a20`, app còn
  66%; flash `/dev/ttyACM0` ở 115200, verify hash pass, không chạy erase/NVS.
- Production Voice revision `87` chạy `wake-test-normal.local.json` **2/2**:
  `wake detected → wake start → server accepted audio → state=speaking →
  graceful tts drain → wake detector armed`; player exit `0`, không forbidden
  panic/queue/Opus marker. Sau drain `active_turns=0`, `protocol_errors=0`,
  `last_ttfa_ms=1448` (Operational TTFA của lượt cuối, không phải p95).
- Acoustic probe dùng snapshot fixture `/tmp` revision `900`, `secretRefs=[]`
  và test key pool; Manager snapshot/database production không bị sửa. Cùng
  port `18100`, firmware log xác nhận `acoustic duplex capture enabled while
  server is speaking` rồi `acoustic barge-in committed; capture kept for new
  auto turn`. Voice metrics: `barge_in_count=3`, `turn_admissions=4`,
  `turn_releases=4`, `active_turns=0`, `protocol_errors=0`, không panic/queue/
  Opus marker. Report redact: `/tmp/veetee-acoustic-barge-20260805.json`.
- Clip interrupt dài hơn thời điểm commit nên phần audio còn lại tạo thêm hai
  interrupt; đây là bằng chứng cancellation/lifecycle, không phải false-positive
  hay time-to-silence pass. Acoustic echo-only, voice-onset corpus, p95
  time-to-silence, false accept/reject, 100-repetition và production promotion
  vẫn mở. Sau probe đã dừng fixture, khởi động lại Voice Manager-source revision
  `87`, board reconnect `activeConnections=1`; không đổi Wi-Fi/NVS/Tailscale và
  không lưu raw audio/transcript/credential vào Git.

## Audio test permission — current direct physical run (2026-08-05)

- Sau khi chủ dự án mở quyền phát audio, production Voice `18100` revision `87`
  chạy `wake-test-normal.local.json` với **2/2** lượt thành công. Mỗi lượt có
  đủ `wake detected → wake start → state=speaking → graceful tts drain complete
  state=listening → wake detector armed`; wake/utterance player đều `exitCode=0`.
  Report redact: `/tmp/veetee-wake-audio-20260805-current-2.json`.
- `wake-barge-in.example.json` chạy **1/1** lượt với interrupt clip phát ngay
  sau `state=speaking`: `wake detected → state=listening → wake interrupt →
  wake start`, player exit `0`; có warning stale `tts/stop` đúng barrier và
  không có panic/stack/queue/Opus marker. Đây là wake-word interrupt của
  firmware, không phải acoustic voice-onset/time-to-silence acceptance.
  Report redact: `/tmp/veetee-wake-barge-audio-20260805-current.json`.
- Negative/no-speech chạy lại sau khi tăng `monitor.startupWaitSeconds` lên `5`
  đạt `wake detected → wake start → NO_SPEECH_TIMEOUT → wake detector armed`,
  player exit `0`, không forbidden marker. Lần thử đầu với chờ `1` giây timeout
  ở marker wake vì IDF monitor chưa attach ổn định trước khi phát; không có
  crash/provider call. Report thành công:
  `/tmp/veetee-wake-no-speech-20260805-current-startup5.json`.
- Các scenario mẫu tracked `wake-test.example.json` và
  `wake-barge-in.example.json` dùng chờ monitor `5` giây để tránh false-fail
  timing; đây là test-harness config, không thay đổi firmware threshold/model,
  wire protocol, Wi-Fi/NVS hay production config. Harness unit/corpus/PTT suite:
  **20 passed**.
- Sau test: Voice `/health/ready` `200`, `active_turns=0`,
  `protocol_errors=0`, `turn_releases=5`; Manager API `18101` và Web `18181`
  vẫn `200`. Acoustic echo-only, voice-onset/time-to-silence, false
  accept/reject corpus, 100-repetition, PTT/LCD/loa physical acceptance và
  TTFA p95 promotion vẫn **mở**.

## Acoustic duplex — direct physical voice-onset probe (2026-08-05)

- Chạy Voice fixture ngoài Git revision `900` trên cùng port `18100` với
  `bargeIn.enabled=true`, `deviceDuplex=true`, `minSpeechFrames=2`, LLM
  `secretRefs=[]` và `VEETEE_TEST_GROQ_KEYS_FILE` (4 key). History/presence
  tắt; Manager snapshot/database production không bị mutate.
- Phát wake phrase, câu tiếng Việt và một clip thoại ngắn làm interrupt. Serial
  xác nhận đúng acoustic path: `acoustic duplex capture enabled while server is
  speaking` → `state=listening generation=16` → `acoustic barge-in committed;
  capture kept for new auto turn`. Player exit `0`; không panic/stack/queue/Opus.
  Report redact: `/tmp/veetee-acoustic-duplex-current-20260805.json`.
- Probe cũng lộ **retrigger/feedback loop** khi AEC/echo-only chưa đủ sạch:
  sau một interrupt, fixture đo `barge_in_count=10`, `turn_admissions=11`,
  `turn_releases=10`, `active_turns=1`, nhiều ASR/LLM request nối tiếp dù clip
  đã kết thúc. Đã dừng fixture ngay, không coi đây là pass time-to-silence hay
  false-accept gate. Đây là bằng chứng cần thêm AEC reference alignment,
  echo-only corpus và một config-driven rearm/loop guard trước khi promote
  acoustic duplex mặc định.
- Khôi phục Voice Manager-source revision `87`; board giữ state speaking cũ
  sau fixture nên dùng `esptool chip-id` với `--after hard-reset` (chỉ reset,
  không flash/erase/NVS). Sau reset: Voice `activeConnections=1`,
  `activeTurns=0`, API/Web ready; firmware vẫn `wake_ready=1`, `capture=0`,
  stack headroom khoảng 31 KB. Không đổi Wi-Fi/route/firewall/Tailscale.
